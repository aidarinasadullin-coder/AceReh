# phase-0-5-model-driven-architecture-widget-v2-amendment-verifier-entrypoints-correction - Work Plan

## TL;DR (For humans)
**What you'll get:** The existing architecture verifier will recognize all five approved amendment verification entrypoints, enforce each entrypoint's own inputs, and preserve separate, failure-safe verification receipts. The two stale snapshot arguments will be corrected through a narrow overlay that points only to the accepted retry snapshot.

**Why this approach:** It repairs the missing executable surface without rewriting the already approved amendment or reopening its accepted work. Existing model and runtime checks remain backward-compatible, while the future amendment verification remains a separate gated action.

**What it will NOT do:** It will not run or approve the five amendment verification suites, change their criteria, alter architecture data or runtime behavior, release the original rendering task, or unblock Phase 1.

**Effort:** Medium
**Risk:** Medium - five heterogeneous verification contracts must coexist in one verifier without confusing historical receipts or crossing workflow gates; authority inputs use trusted-local accidental-drift detection rather than claiming active-filesystem-attacker resistance.
**Decisions to sanity-check:** The approved amendment stays byte-identical; only the two stale snapshot arguments are superseded; availability is not verification approval; the owner selected the cross-platform trusted-local threat model described below.

Your next move: use the reviewed plan in a separate authorized worker session; do not treat plan approval as execution authorization. Full execution detail follows below.

---

> TL;DR (machine): Medium effort, medium risk; add suite-aware CLI dispatch and five independent amendment handlers to the existing verifier, preserve old suites, and record a correction-only handoff without running amendment F1-F5.

## Scope
### Must have
- Preserve these authorities byte-for-byte: `docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment.md`, accepted amendment Tasks 1-5 receipts, `docs/architecture-migration/evidence/phase-0.5-v2-amendment-repository-snapshot.md` as failed history, and `docs/architecture-migration/evidence/phase-0.5-v2-amendment-repository-snapshot-retry-1.md` as the sole accepted Task 1 authority.
- After byte-identical import, the sole repository-facing correction-plan authority is `docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment-verifier-entrypoints-correction.md`; its imported bytes/SHA, owner approval, execution authorization, Todo 1 gate, Todo 8 overlay record, final audits, commit boundary, rollback boundary, and success criteria must all bind to that path. `.omo/plans/phase-0-5-v2-amendment-verifier-entrypoints-correction.md` remains only the primary Prometheus source artifact and is never an execution-time authority or required QA input.
- Threat model: this correction runs in a trusted local environment and detects accidental authority-input drift across platforms. It does not claim protection against an active local filesystem attacker. Active filesystem attacks, intentional TOCTOU substitution, and malicious symlink/junction/reparse-point races are explicitly out of scope.
- Every authority input, including the canonical imported correction plan and F5's four canonical receipts, must use this exact cross-platform accidental-drift procedure with ordinary filesystem APIs and no native extension: (1) resolve the candidate with `realpath`; (2) require the resolved path to be contained within the canonical repository root using `path.relative(root, resolved)` and reject an absolute result, `..`, or a result beginning `..` plus the platform separator; (3) capture a pre-read `stat` and require a regular file; (4) perform one complete byte read and compute SHA-256 from those bytes; (5) capture a post-read `stat`; (6) reject any change in `dev`, `ino`, `size`, or `mtimeMs`; (7) reject any mismatch against the expected SHA-256; and (8) fail closed without creating or accepting a receipt on any discrepancy. If a platform reports unavailable identity fields, normalize only that platform's documented unavailable value consistently; never omit comparison for a field that is reported by either stat.
- Modify the implementation only in `docs/architecture-migration/widget/verify-widget.mjs`; add exactly `v2-amendment-f1`, `v2-amendment-f2`, `v2-amendment-f3`, `v2-amendment-f4`, and `v2-amendment-f5` to its canonical dispatch.
- Keep `model-v2` and `runtime-v2` command lines, output shape, assertion IDs/totals, negative mutations, and exit behavior backward-compatible. A changed verifier SHA is expected metadata, not a behavioral failure.
- Validate arguments after identifying the suite. Required matrices are: F1=`--plan --context --output`; F2=`--schema --model --mapping --v1-receipt --output`; F3=`--schema --model --output`; F4=`--snapshot --context --output`; F5=`--receipts --snapshot --context --output`; existing suites retain `--schema --model --output`.
- Reject odd argument counts, duplicate flags, unknown flags, empty values, unknown suites, missing/non-regular inputs, unsafe output types, and irrelevant cross-suite flags deterministically before a semantic handler writes output. One invocation may reach at most one handler.
- Implement each F-suite's independent criteria from `docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment.md:128-142`; shared low-level helpers are allowed, but F2/F3 must not be aliases for `model-v2`/`runtime-v2` and F5 must not accept arbitrary PASS-like files.
- F1-F4 each own only their caller-provided distinct receipt; F5 reads exactly four distinct canonical amendment F1-F4 regular files and owns only its aggregate. All new receipts use UTF-8/LF, include command/cwd/UTC/Node version/input hashes/assertion totals/failures/blockers/write-set, and end in exactly one `Terminal verdict: APPROVE|REJECT` (F1-F4) or `Aggregate verdict: APPROVE|REJECT` (F5).
- New receipt writes must use temporary-file-plus-atomic-rename semantics. Any parse, input, assertion, type, identity, hash, or verdict failure must leave every pre-existing output byte-identical and remove task-temporary artifacts.
- Supersede only the F4/F5 `--snapshot` token from the failed initial receipt to `docs/architecture-migration/evidence/phase-0.5-v2-amendment-repository-snapshot-retry-1.md`; reject the initial failed receipt explicitly and never search, infer, or fall back to another snapshot.
- Correction QA may run only `model-v2`, `runtime-v2`, syntax/import checks, malformed/incomplete F-suite invocations that stop before semantic execution, and task-temporary fixture probes. Canonical `v2-amendment-f1..f5` semantic runs and canonical F-receipt creation are a later workflow action.
- Allowed repository writes during correction execution are only `docs/architecture-migration/widget/verify-widget.mjs`, `docs/architecture-migration/evidence/phase-0.5-v2-amendment-verifier-entrypoints-correction.md`, and factual current-state/decision-log sections of `docs/architecture-migration/TASK_CONTEXT.md`.
- After correction completion, record entrypoints as available while amendment F1-F5 remain unstarted, original Phase 0.5 Task 5 remains blocked, Phase 1 remains blocked, and phase result acceptance remains pending.
### Must NOT have (guardrails, anti-slop, scope boundaries)
- Do not edit the approved amendment plan to repair the stale paths; the imported repository correction plan at `docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment-verifier-entrypoints-correction.md` is the sole narrow supersession overlay and must remain byte-identical after import.
- Do not alter F1-F5 criteria, dependencies, canonical output names, assertion meaning, or accepted amendment Tasks 1-5.
- Do not create standalone verifiers, a second runtime/model source, browser harnesses, generated HTML, screenshots, UI changes, schema/model/runtime edits, packages, installs, production/test/config/release changes, or waivers.
- Do not execute semantic `v2-amendment-f1..f5`, create their canonical receipts, aggregate temporary fake approvals, release original Task 5, infer owner acceptance, or unblock Phase 1.
- Do not use `git reset`, `git clean`, `git stash`, broad restore, staging, commit, or any operation that touches unrelated dirty paths.
- Do not accept an authority input whose resolved path is outside the canonical repository root, a directory/non-regular file, a file that disappears, changes `dev`/`ino`/`size`/`mtimeMs` between checks, or has the wrong expected SHA; also reject duplicate receipt identities, Phase 0 `final-verification*.md` receipts, a mutable `TASK_CONTEXT.md` whole-file hash as snapshot authority, and the failed initial snapshot as F4/F5 authority.
- Do not require or add descriptor-relative traversal, same-descriptor hashing, native helpers, FFI, new dependencies, or an unimplementable no-follow chain. Ordinary `realpath`, containment, pre/post `stat`, one byte read and expected-SHA comparison are the complete owner-selected authority-input contract.

## Verification strategy
> Zero human intervention - all verification is agent-executed.
- Test decision: tests-after with Node's built-in runtime, `node --check`, direct CLI probes, SHA-256 comparisons, and temporary directories under `os.tmpdir()`; no package install and no new standalone test harness.
- Evidence: all per-task raw command/output goes outside the repository under `%TEMP%\ace-v2-verifier-entrypoints-correction\<execution-id>\task-<N>.{json,txt}`. The only repository evidence is the consolidated `docs/architecture-migration/evidence/phase-0.5-v2-amendment-verifier-entrypoints-correction.md`; `.omo/evidence/` is not an execution write path.
- Baseline: before edits capture Git root/HEAD/status, verifier SHA-256, approved amendment SHA-256, canonical imported correction-plan path/SHA-256, retry-1 receipt SHA-256, scope-gate terminal PASS, all protected artifact hashes, and absence/unstarted status of amendment F1-F5. Validate the imported correction plan with the exact eight-step `realpath`/containment/pre-stat/read+SHA/post-stat/stable-identity/expected-SHA/fail-closed procedure in Scope. Any mismatch stops with factual `blocked` context and no acceptance receipt; no edits follow.
- Correction-only positive QA: run existing `model-v2` and `runtime-v2` against task-temporary outputs and require exit `0`, unchanged assertion IDs/totals/status, no canonical output drift, and no F-suite dispatch.
- Correction-only availability QA: invoke each F-suite with a deliberately omitted suite-specific required input and require nonzero deterministic `cli-arguments` detail naming that suite's missing input, no semantic receipt, and byte-identical canonical artifacts. This proves dispatch/argument reachability, not amendment approval.
- Failure QA: cover unknown suite/flag, odd count, duplicate/empty flag, irrelevant cross-suite flag, unsafe output, stale initial snapshot, and every canonical F1-F5 mutation ID enumerated below; authority-input QA must additionally cover the five required cross-platform negative probes: outside-root resolved path, directory/non-regular file, mutation between pre/post checks, expected-SHA mismatch, and disappearance between checks. F5 coverage is all twelve semantic cases plus these authority-input probes, not only duplicate/missing receipts. Every probe must stop before canonical semantic approval, create no acceptance receipt, and preserve output hashes.
- Future F-wave commands are documented in the handoff but explicitly not run here; their success criteria remain the approved amendment plan's F1-F5 criteria.
- Testability contract: `verify-widget.mjs` must additionally export frozen `suiteContracts`, `parseCli`, and pure/injected-I/O `verifyAmendmentF1`…`verifyAmendmentF5` functions. CLI `run()` delegates to these same functions. Tests import these exports via `node --input-type=module -e "import('./docs/architecture-migration/widget/verify-widget.mjs').then(...)"`, pass synthetic task-temporary fixtures and injected readers/stat/hash providers, and never invoke a semantically complete canonical F CLI command.
- Receipt grammar: F1-F4 Markdown is UTF-8/LF and contains, in this exact order, H1 suite name; one fenced `json` object; one final `Terminal verdict: APPROVE|REJECT` line and no other verdict line. F5 uses the same shape with final `Aggregate verdict: APPROVE|REJECT`. JSON keys are exactly `contract_version`=`1`, `suite`, `command` (argv string array), `cwd`, `timestamp_utc`, `node_version`, `inputs` (sorted path keys to lowercase 64-hex SHA-256), `output`, `assertions` (`total`,`passed`,`failed`,`ids`), `mutations` (`total`,`passed`,`failed`,`ids`), `unresolved_blockers` (string array), `generic_validation` (`status`=`pass|degraded`,`detail`), `write_set` (sorted path array), `bindings` (`amendment_sha256`,`snapshot_path`,`snapshot_basis_sha256`), and `verdict`. F5 parses this grammar with bounded sizes and requires JSON verdict plus terminal line to agree.
- Canonical IDs: F1 assertions=`f1-plan-sha`,`f1-five-tasks`,`f1-dependencies`,`f1-supersession`,`f1-owner-approval`,`f1-execution-authorization`,`f1-history-preserved`,`f1-fwave-unstarted`; mutations=`f1-missing-task`,`f1-inverted-dependency`,`f1-plan-hash-drift`,`f1-missing-owner-gate`,`f1-premature-task5`. F2 assertions=`f2-input-hashes`,`f2-model-suite`,`f2-reconciliation`,`f2-six-views`,`f2-five-kinds`,`f2-global-ids`,`f2-snapshot-local-endpoints`,`f2-snapshot-local-flows`,`f2-target-honesty`,`f2-no-legacy-authority`,`f2-policy-complete`,`f2-zero-unmapped`; mutations=`f2-source-hash-drift`,`f2-lost-field-id`,`f2-fabricated-target`,`f2-duplicate-id`,`f2-orphan-endpoint`,`f2-global-only-flow`,`f2-retained-snapshots`,`f2-policy-invalid`. F3 assertions=`f3-five-directions`,`f3-five-kinds`,`f3-swap-symmetry`,`f3-immutable-aliases`,`f3-failed-replacement-identity`,`f3-policy-driven`,`f3-empty-matrix`; mutations=`f3-unchanged-defect`,`f3-duplicate-overwrite`,`f3-hardcoded-policy`,`f3-partial-commit`,`f3-second-source`,`f3-filtered-target`. F4 assertions=`f4-retry-authority`,`f4-protected-immutable`,`f4-accepted-deltas`,`f4-correction-deltas`,`f4-dirty-preserved`,`f4-no-forbidden-deltas`,`f4-workflow-gates`; mutations=`f4-protected-drift`,`f4-absent-collision`,`f4-extra-path`,`f4-v1-receipt-drift`,`f4-generated-artifact`,`f4-crossed-gate`. F5 assertions=`f5-four-distinct`,`f5-trusted-local-authority-input`,`f5-canonical-order`,`f5-bindings`,`f5-four-approvals`,`f5-positive-totals`,`f5-zero-failures`,`f5-degraded-disclosed`,`f5-workflow-blocked`; mutations=`f5-missing-receipt`,`f5-duplicate-receipt`,`f5-directory-receipt`,`f5-authority-input-drift`,`f5-metadata-drift`,`f5-hash-drift`,`f5-duplicate-verdict`,`f5-zero-assertions`,`f5-unresolved-blocker`,`f5-protected-drift`,`f5-extra-receipt`,`f5-premature-state`. The renamed F5 IDs replace the superseded no-follow/symlink semantics; they assert only the owner-selected eight-step trusted-local contract and do not reject a symlink merely because it is a symlink when ordinary `realpath` resolves it to a contained regular file whose identity and SHA remain stable.
- Path/size policy: canonical F outputs are exactly the five amendment paths in the approved plan, except F4/F5 use retry-1 input; test outputs must be descendants of `%TEMP%\ace-v2-verifier-entrypoints-correction\<execution-id>`. Reject every other repository output, an existing non-owned destination, and an output aliasing any input/protected path. For every authority input, use ordinary `realpath`, canonical-root containment, regular-file pre-stat, one byte read plus SHA-256, post-stat stable `dev`/`ino`/`size`/`mtimeMs`, and expected-SHA comparison; fail closed without an acceptance receipt on any discrepancy. This policy detects accidental trusted-local drift only and deliberately makes no active-attacker or reparse-race claim. Cap each input/receipt at 2 MiB, `--receipts` at four entries/8192 characters, and JSON nesting at the parser's ordinary JSON representation without recursive traversal beyond required fields.
- Atomic policy: create a same-directory `<destination>.tmp-<pid>-<128-bit-random-hex>` with exclusive `wx`, write/flush/close, then rename only when destination does not exist; canonical receipt destinations are create-once and an existing destination is rejected byte-preserving. On every failure close and remove only the owned temporary. Do not rely on rename-over-existing behavior on Windows.

## Execution strategy
### Parallel execution waves
> Target 5-8 todos per wave. Fewer than 3 (except the final) means you under-split.
- Wave 1: Todo 1 only — freeze prerequisites and correction boundary.
- Wave 2: Todo 2 only — establish the shared suite-aware parser/dispatch seam.
- Wave 3: Todos 3, 4, 5, and 6 run sequentially in one implementation lane because all modify the same verifier file.
- Wave 4: Todo 7 after Todos 3-6 because F5 consumes all four receipt contracts.
- Wave 5: Todo 8 after all implementation todos — correction-only regressions, consolidated evidence, and factual handoff.

### Dependency matrix
| Todo | Depends on | Blocks | Can parallelize with |
| --- | --- | --- | --- |
| 1 | none | 2, 6, 8 | none |
| 2 | 1 | 3, 4, 5, 6 | none |
| 3 | 2 | 4, 7, 8 | none |
| 4 | 3 | 5, 7, 8 | none |
| 5 | 4 | 6, 7, 8 | none |
| 6 | 1, 5 | 7, 8 | none |
| 7 | 3, 4, 5, 6 | 8 | none |
| 8 | 1-7 | final verification | none |

## Todos
> Implementation + Test = ONE todo. Never separate.
<!-- APPEND TASK BATCHES BELOW THIS LINE WITH edit/apply_patch - never rewrite the headers above. -->
- [ ] 1. Freeze prerequisites, hashes, write boundary, and the two-token supersession overlay
  What to do / Must NOT do: Read the canonical Git root, HEAD, complete porcelain status, current verifier, approved amendment, retry-1 receipt, failed initial receipt, amendment scope-gate, exact canonical imported correction plan at `docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment-verifier-entrypoints-correction.md`, and `TASK_CONTEXT.md`. Record exact paths/sizes/SHA-256 values, scope-gate PASS, F1-F5 unstarted, three correction-execution write paths, the imported correction plan as immutable protected authority, and the two-token supersession table. Validate the imported plan with the exact eight-step trusted-local authority-input procedure from Scope and require its SHA to equal the separately owner-approved/imported SHA recorded in `TASK_CONTEXT.md`, `Stage=approved`, and a later separate `/architecture-start phase-0-5-model-driven-architecture-widget-v2-amendment-verifier-entrypoints-correction` authorization recording the same repository path/SHA and transitioning to `Stage=executing`. The `.omo` source path and old amendment authorization are insufficient. Stop without an acceptance receipt if realpath containment, regular-file status, stable pre/post `dev`/`ino`/`size`/`mtimeMs`, expected SHA, any gate, import identity, or hash differs. Do not add descriptor traversal, same-descriptor hashing, native helpers, FFI, dependencies, or active-attacker claims.
  Parallelization: Wave 1 | Blocked by: none | Blocks: 2, 6, 8
  References (executor has NO interview context - be exhaustive): `docs/architecture-migration/AGENTS.md:18-27,63-95`; `docs/architecture-migration/TASK_CONTEXT.md:344-388,476-482,500-520,552-571`; `docs/architecture-migration/evidence/phase-0.5-v2-amendment-scope-gate.md:7-10,43-70`; `docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment.md:25-36,128-142,164`.
  Acceptance criteria (agent-executable): PowerShell/Python read-only script writes `%TEMP%\ace-v2-verifier-entrypoints-correction\<execution-id>\task-1.json` only after the trusted-local check passes, containing root/HEAD, amendment SHA `b6df07e7b150f6830a3ee3a4cdba9b441d05e5c4f475a594dc1c83208d5f3536`, pre-edit verifier SHA `2d281d7c46adfd987f26227bbf8e2edd15b1dbc1edbec213bb98dead7e7c5314`, canonical repository correction-plan resolved path/SHA equal to the context import, pre/post `dev`/`ino`/`size`/`mtimeMs`, approval and start receipts, regular-file status, `Stage=executing`, scope gate PASS, retry-1 authority, exactly three correction-execution writes, and the imported correction plan in the immutable protected set; exits nonzero before edits and creates no PASS/acceptance receipt on any mismatch or `.omo` authority reference.
  QA scenarios (name the exact tool + invocation): Happy — run a standard-library-only PowerShell/Python/Node authority-input script against the canonical repository plan and require `prerequisites=PASS`. Failure — with task-temporary injected filesystem adapters or fixture orchestration, independently probe (1) outside-root resolved path, (2) directory, (3) changed `dev`/`ino`/`size`/`mtimeMs` between pre/post stat, (4) wrong expected SHA, and (5) disappearance before post-stat; each exits nonzero, emits only non-acceptance diagnostic evidence under `%TEMP%`, creates no PASS receipt, and changes no repository hash. Also point a temporary fixture at the failed initial snapshot and require `blocked`. Evidence `%TEMP%\ace-v2-verifier-entrypoints-correction\<execution-id>\task-1.{json,txt}`.
  Commit: N | Planning workflow forbids commit/staging; preserve owner dirty worktree.

- [ ] 2. Replace the global CLI guard with deterministic suite-aware parsing and single-dispatch routing
  What to do / Must NOT do: In `verify-widget.mjs`, parse complete key/value pairs; reject odd counts, duplicate flags, unknown flags, empty values, unknown suites, and flags irrelevant to the selected suite. Select the suite before loading files, validate only its required matrix, and ensure one invocation reaches at most one handler. Preserve the existing two suite paths exactly. Expose no new standalone CLI or dependency.
  Parallelization: Wave 2 | Blocked by: 1 | Blocks: 3, 4, 5, 6
  References (executor has NO interview context - be exhaustive): `docs/architecture-migration/widget/verify-widget.mjs:8-12,47-55`; approved commands at `docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment.md:130,133,136,139,142`; current blocker at `docs/architecture-migration/TASK_CONTEXT.md:504-514,572-582`.
  Acceptance criteria (agent-executable): `node --check docs/architecture-migration/widget/verify-widget.mjs` exits 0; a table-driven temporary PowerShell/Node probe invokes all seven suite names with one omitted required field and confirms suite-specific `cli-arguments` details; unknown suite/flag, odd count, duplicate/empty flag and irrelevant flag all exit 1 without creating output; canonical model/runtime outputs and F receipts retain pre-probe hashes.
  QA scenarios (name the exact tool + invocation): Happy — canonical `model-v2` and `runtime-v2` invocations with output redirected to `os.tmpdir()` reach their original handlers. Failure — `node .../verify-widget.mjs --suite v2-amendment-f1 --output <tmp>` reports missing `plan, context` rather than `schema, model`; `--suite nope` never falls through to runtime. Evidence `%TEMP%\ace-v2-verifier-entrypoints-correction\<execution-id>\task-2.{json,txt}`.
  Commit: N | No staging/commit in this migration workflow.

- [ ] 3. Implement the independent F1 plan/dependency/gate compliance handler
  What to do / Must NOT do: Add F1 logic that reads only `--plan` and `--context`, binds the exact amendment SHA, verifies all five implementation tasks/dependencies, supersession declarations, owner plan approval, separate execution authorization, preserved accepted history, and unstarted F-wave state, then atomically writes only the caller's F1 output. Implement the approved five temporary negative mutations. Do not edit plan/context and do not reuse Phase 0 F1 receipts.
  Parallelization: Wave 3 sequential lane | Blocked by: 2 | Blocks: 4, 7, 8
  References (executor has NO interview context - be exhaustive): `docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment.md:38-48,68-77,128-130`; `docs/architecture-migration/TASK_CONTEXT.md:329-336,367-378,974-1014`; `docs/architecture-migration/evidence/final-verification-f1-plan-compliance.md` as forbidden historical namespace.
  Acceptance criteria (agent-executable): import `suiteContracts,verifyAmendmentF1` with `node --input-type=module`; pass synthetic temporary plan/context plus injected I/O and assert all listed F1 IDs, APPROVE receipt grammar/counts, each five mutation's REJECT, temp cleanup and unchanged pre-existing destination. Incomplete canonical CLI probe exits 1 before semantics. Canonical semantic F1 is not run.
  QA scenarios (name the exact tool + invocation): Happy — import and execute `verifyAmendmentF1` with synthetic fixtures; require APPROVE plus exact listed IDs. Failure — execute each listed mutation plus omitted context/directory/collision and require REJECT, cleanup and byte preservation. Evidence `%TEMP%\ace-v2-verifier-entrypoints-correction\<execution-id>\task-3.{json,txt}`.
  Commit: N | No staging/commit.

- [ ] 4. Implement the independent F2 schema/mapping/contract-quality handler
  What to do / Must NOT do: Add F2 logic for schema/model/mapping/v1-receipt hashes, v1 source precedence, complete 16/245/112/280/355 reconciliation, six views, five record kinds, global IDs, snapshot-local endpoints/flows, Target honesty, no legacy authority, and complete non-contradictory model-driven policy. Include all eight approved temporary negative mutations. Reuse validators only as low-level evidence; do not alias `model-v2` or weaken generic-validator degraded disclosure.
  Parallelization: Wave 3 sequential lane | Blocked by: 3 | Blocks: 5, 7, 8
  References (executor has NO interview context - be exhaustive): `docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment.md:58-62,98-104,132-133`; `docs/architecture-migration/widget/model-contract.mjs:1-47`; `docs/architecture-migration/evidence/phase-0.5-v1-to-v2-mapping.json`; `docs/architecture-migration/evidence/phase-0.5-model-validation-v2.md`; `docs/architecture-migration/widget-spec.md:35-206`.
  Acceptance criteria (agent-executable): import `verifyAmendmentF2`; use synthetic 16/245/112/280/355-bound fixture copies and injected I/O to assert all listed F2 IDs, APPROVE grammar/counts/degraded disclosure, eight independent REJECT mutations, cleanup and output preservation. Incomplete canonical CLI exits 1. Canonical semantic F2 is not run.
  QA scenarios (name the exact tool + invocation): Happy — import and execute `verifyAmendmentF2` with synthetic bound fixture copies; require APPROVE, exact IDs and degraded disclosure. Failure — execute each listed mutation plus omitted/non-file/collision cases and require REJECT, cleanup and byte preservation. Evidence `%TEMP%\ace-v2-verifier-entrypoints-correction\<execution-id>\task-4.{json,txt}`.
  Commit: N | No staging/commit.

- [ ] 5. Implement the independent F3 runtime/immutability/Diff handler
  What to do / Must NOT do: Add F3 logic for all five directions and changed-kind fixtures, swap symmetry, deep immutable aliases, identical-state failed replacement, model-driven policy mutation, full empty/no-match matrix, and six approved negative runtime mutations. Shared runtime functions are allowed, but F3 must carry its own suite identity/assertion matrix and must not alias `runtime-v2`.
  Parallelization: Wave 3 sequential lane | Blocked by: 4 | Blocks: 6, 7, 8
  References (executor has NO interview context - be exhaustive): `docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment.md:105-115,134-136`; `docs/architecture-migration/widget/architecture-widget.mjs:56-89`; `docs/architecture-migration/widget/verify-widget.mjs:24-45`; `docs/architecture-migration/evidence/phase-0.5-acceptance-v2.json`.
  Acceptance criteria (agent-executable): import `verifyAmendmentF3`; use synthetic cloned schema/model fixtures and injected writer to execute all listed F3 assertions and six REJECT mutations, verify receipt grammar/counts, cleanup and destination preservation. Incomplete canonical CLI exits 1. Canonical semantic F3 is not run.
  QA scenarios (name the exact tool + invocation): Happy — import and execute `verifyAmendmentF3` against synthetic schema/model and require APPROVE with exact IDs. Failure — execute six listed mutations plus missing/unsafe/irrelevant inputs and require REJECT, cleanup and byte preservation. Evidence `%TEMP%\ace-v2-verifier-entrypoints-correction\<execution-id>\task-5.{json,txt}`.
  Commit: N | No staging/commit.

- [ ] 6. Implement F4 scope/history verification bound only to the accepted retry-1 snapshot
  What to do / Must NOT do: Add F4 logic that classifies retry-1 rows into (a) immutable protected paths: approved plans, v1 receipts, widgets, archives, production/tests/config/release/unrelated user dirty paths; (b) accepted amendment deltas: the ten Task 2-5 allow-list paths and accepted receipts from scope-gate; (c) correction deltas: exactly the three correction write paths; (d) pre-existing dirty identities preserved by porcelain identity/content rule; and (e) every other delta forbidden. Parse only exact context fields `Current phase`,`Stage`,`Active plan`,`Parent execution state`,`Phase result acceptance`,`F1-F5`,`Original Phase 0.5 Task 5`,`Phase 1`; do not compare whole context hash. Require executing correction state values defined in Todo 1 and reject the failed snapshot/fallback.
  Parallelization: Wave 3 sequential lane | Blocked by: 1, 5 | Blocks: 7, 8
  References (executor has NO interview context - be exhaustive): stale commands `docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment.md:137-142`; authority `docs/architecture-migration/TASK_CONTEXT.md:552-571,619-626`; `docs/architecture-migration/evidence/phase-0.5-v2-amendment-repository-snapshot-retry-1.md`; `docs/architecture-migration/evidence/phase-0.5-v2-amendment-scope-gate.md:7-10,39-70`; failed history `docs/architecture-migration/evidence/phase-0.5-v2-amendment-repository-snapshot.md`.
  Acceptance criteria (agent-executable): import `verifyAmendmentF4`; feed synthetic retry ledger/live-status/context through injected readers and execute one APPROVE plus all six listed REJECT mutations, asserting category counts, exact context fields, receipt grammar, cleanup and destination preservation. Incomplete canonical CLI with failed snapshot exits 1. Canonical semantic F4 is not run.
  QA scenarios (name the exact tool + invocation): Happy — import and execute `verifyAmendmentF4` with synthetic categorized ledger/live/context and require APPROVE with exact IDs. Failure — execute six listed mutations plus initial snapshot, outside-root resolved input, directory, between-check mutation, wrong expected SHA and disappearance; require REJECT, no acceptance receipt, cleanup and unchanged receipts. Evidence `%TEMP%\ace-v2-verifier-entrypoints-correction\<execution-id>\task-6.{json,txt}`.
  Commit: N | No staging/commit.

- [ ] 7. Implement F5 strict four-receipt aggregation and failure-safe ownership
  What to do / Must NOT do: Parse `--receipts` as exactly four comma-separated canonical amendment F1-F4 paths. For each receipt, apply the exact trusted-local `realpath` containment, regular pre-stat, one-read SHA, stable post-stat identity and expected-SHA procedure; then require four distinct resolved regular files, correct namespace/order, matching amendment/retry-1 bindings, exactly one terminal APPROVE each, positive totals, zero failures/blockers, and degraded-validator disclosure. Atomically write only the aggregate after all checks. Implement all approved missing/duplicate/type/metadata/hash/verdict/zero-total/blocker/protected-byte/extra-path/premature-state negatives plus outside-root, directory, between-check mutation, expected-hash mismatch and disappearance probes. Do not update `TASK_CONTEXT.md` inside the verifier, accept Phase 0 receipts, add native/FFI/dependency code, or claim protection against active local races.
  Parallelization: Wave 4 | Blocked by: 3, 4, 5, 6 | Blocks: 8
  References (executor has NO interview context - be exhaustive): `docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment.md:58-62,140-142,153-164`; pre-existing forbidden namespace `docs/architecture-migration/evidence/final-verification*.md`; workflow gates `docs/architecture-migration/TASK_CONTEXT.md:500-520`.
  Acceptance criteria (agent-executable): import `verifyAmendmentF5`; create four synthetic grammar-conformant temporary receipts and injected `realpath`/`stat`/read/hash/write providers, execute one APPROVE aggregation plus all twelve listed semantic REJECT mutations and all five authority-input negatives, and assert bounded parsing, exact ordering, canonical-root containment, regular-file pre-stat, one-read digest, stable post-stat identity, expected-SHA binding, fail-closed cleanup and aggregate preservation. Incomplete canonical CLI exits 1 and context is never written. Canonical semantic F5 is not run.
  QA scenarios (name the exact tool + invocation): Happy — import and execute `verifyAmendmentF5` with four synthetic grammar-valid trusted-local receipts and require aggregate APPROVE with exact IDs. Failure — execute all twelve listed mutations plus outside-root path, directory, mutation between checks, hash mismatch and disappearance; each must REJECT, create no aggregate acceptance receipt, clean owned temporaries and preserve aggregate/context. Evidence `%TEMP%\ace-v2-verifier-entrypoints-correction\<execution-id>\task-7.{json,txt}`.
  Commit: N | No staging/commit.

- [ ] 8. Run correction-only regressions, publish one correction receipt, and record a factual blocked handoff
  What to do / Must NOT do: Recompute protected hashes/status, including the immutable imported correction-plan hash, run correction-only QA, and write the consolidated receipt. Append one factual decision-log entry and set exact post-correction fields: `Current phase=phase-0.5-model-driven-architecture-widget`; `Stage=executing`; `Active plan=docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget.md` with its existing SHA; `Correction overlay=docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment-verifier-entrypoints-correction.md` with the owner-approved/imported/executed SHA; `Parent execution state=blocked pending amendment F1-F5`; `Last completed action=verifier entrypoint correction PASS`; `Next action=separately authorize and run amendment F1-F4, then F5`; `Phase result acceptance=pending`; `F1-F5=unstarted`; `Original Phase 0.5 Task 5=blocked`; `Phase 1=blocked`. Do not record `.omo` as authority, enter amendment verification, infer acceptance, or release any task.
  Parallelization: Wave 5 | Blocked by: 1-7 | Blocks: final verification
  References (executor has NO interview context - be exhaustive): existing commands `docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment.md:104,130,133,136,139,142`; current workflow `docs/architecture-migration/TASK_CONTEXT.md:344-388,500-520`; output semantics `docs/architecture-migration/widget/verify-widget.mjs:47-55`.
  Acceptance criteria (agent-executable): `node --check` exits 0; temporary `model-v2` exits 0 with 33 assertions/21 mutations and unchanged IDs; temporary `runtime-v2` exits 0 with 47 total assertions/20 mutations and unchanged suite IDs; each F-suite incomplete probe exits 1 with suite-specific missing args and no canonical receipt; consolidated receipt ends `Terminal verdict: PASS`; `git status --porcelain=v1 -z --untracked-files=all` shows no correction-execution change beyond the three allow-listed paths relative to Todo 1; the canonical imported correction plan remains byte-identical to its Todo 1 SHA; context contains the canonical repository overlay path/SHA and all five blocked/pending facts, with no `.omo` authority reference.
  QA scenarios (name the exact tool + invocation): Happy — run the exact two existing commands with `%TEMP%` outputs, hash all protected artifacts before/after, and validate the consolidated receipt/context fields. Failure — deliberately pass the failed snapshot to incomplete F4/F5 probes and a Phase 0 receipt to incomplete F5; require nonzero, no semantic execution, no canonical F receipt and no context release. Evidence `%TEMP%\ace-v2-verifier-entrypoints-correction\<execution-id>\task-8.{json,txt}` plus the versioned correction receipt.
  Commit: N | No staging/commit; repository import/commit remains an owner-controlled architecture workflow action.

## Final verification wave
> Runs in parallel after ALL todos. ALL must APPROVE. Surface results and wait for the user's explicit okay before declaring complete.
- [ ] F1. Plan compliance audit — Tool/invocation: create `%TEMP%\ace-v2-verifier-entrypoints-correction\<execution-id>\final-f1-audit.ps1` from the canonical repository plan's Scope/Dependency matrix using standard-library filesystem, hashing, regex and JSON operations; run `powershell -NoProfile -ExecutionPolicy Bypass -File <script> -Plan docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment-verifier-entrypoints-correction.md -Receipt docs/architecture-migration/evidence/phase-0.5-v2-amendment-verifier-entrypoints-correction.md -Context docs/architecture-migration/TASK_CONTEXT.md -Out <temp>\final-f1-plan-compliance.md`. The script must require `-Plan` to equal that canonical repository path, validate it through the exact eight-step trusted-local authority-input procedure and Todo 1 approved/imported SHA, enumerate all 8 todos/4 finals, compare every Must Have/three-path correction write-set/two-token supersession/exact post-state against receipt+context, reject any `.omo` plan authority reference, and scan recorded commands to prove no semantically complete `--suite v2-amendment-f[1-5]` invocation. It must exercise outside-root, directory, between-check mutation, expected-SHA mismatch and disappearance through task-temporary adapters/fixtures. Exit 0 and terminal `APPROVE` only when every positive and negative check is true; otherwise exit 1 without creating an acceptance receipt. Evidence `%TEMP%\ace-v2-verifier-entrypoints-correction\<execution-id>\final-f1-plan-compliance.md`.
- [ ] F2. Code quality and security review — Tool/invocation: run `node --check docs/architecture-migration/widget/verify-widget.mjs`, then write `%TEMP%\...\final-f2-review.mjs` that imports `{suiteContracts,parseCli,verifyAmendmentF1,verifyAmendmentF2,verifyAmendmentF3,verifyAmendmentF4,verifyAmendmentF5}` and executes table assertions for frozen contracts, seven exact suite names, all required/forbidden flags, 2 MiB/8192/four-receipt bounds, independent function identities, and injected-I/O authority checks for realpath containment, regular pre-stat, one-read SHA, stable post-stat `dev`/`ino`/`size`/`mtimeMs`, expected SHA, output alias and existing destination. It must prove outside-root, directory, mutation-between-checks, hash-mismatch and disappearance each fail closed without an acceptance receipt, and prove there is no native helper, FFI or new dependency. Run `node <temp>\final-f2-review.mjs > <temp>\final-f2-code-quality.md`; require both commands exit 0, all named assertions PASS, temporary cleanup and terminal `APPROVE`; any missing export/assertion/write produces exit 1/`REJECT`. Do not call CLI `run()` with complete F inputs. Evidence `%TEMP%\ace-v2-verifier-entrypoints-correction\<execution-id>\final-f2-code-quality.md`.
- [ ] F3. Real agent-executed correction QA — Tool/invocation: run `node docs/architecture-migration/widget/verify-widget.mjs --suite model-v2 --schema docs/architecture-migration/maps/architecture-model.widget.schema.json --model docs/architecture-migration/maps/architecture-model.json --output %TEMP%\...\model-v2.json` and the analogous `--suite runtime-v2 ... --output %TEMP%\...\runtime-v2.json`; then run `%TEMP%\...\final-f3-probes.mjs`, importing `parseCli` and all five pure handlers, against synthetic temporary fixtures for one APPROVE and every canonical mutation ID listed in Verification strategy. Require old-suite exits 0 and exact 33/21 plus 47/20 totals/IDs, all synthetic F positives APPROVE, all mutations REJECT, canonical F paths absent/unchanged, and all temporary owned files removed. The script writes terminal `APPROVE` and exits 0 only on complete success; otherwise `REJECT`/1. It must not call a complete F CLI command. Evidence `%TEMP%\ace-v2-verifier-entrypoints-correction\<execution-id>\final-f3-real-qa.md`.
- [ ] F4. Scope fidelity — Tool/invocation: generate `%TEMP%\...\final-f4-scope.ps1`; run `git status --porcelain=v1 -z --untracked-files=all` and validate every Todo 1 protected authority, including `docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment-verifier-entrypoints-correction.md`, with the exact trusted-local eight-step procedure before comparing against Todo 1 JSON and the three correction-execution write paths. Parse `TASK_CONTEXT.md` fields and require exact Todo 8 post-state with that canonical repository overlay path/SHA; assert the imported correction plan, approved amendment, retry-1, failed history and Task 1-5 receipts byte-identical; assert canonical amendment F1-F5 receipts absent or byte-identical pre-existing unstarted state; reject outside-root resolution, non-regular file, between-check identity drift, expected-SHA mismatch, disappearance, imported-plan drift, `.omo` authority reference, or fourth correction-execution delta without writing an APPROVE receipt. Run `powershell -NoProfile -ExecutionPolicy Bypass -File <script> -Baseline <temp>\task-1.json -Plan docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment-verifier-entrypoints-correction.md -Context docs/architecture-migration/TASK_CONTEXT.md -Out <temp>\final-f4-scope-fidelity.md`; exit 0 + terminal `APPROVE` iff canonical realpath/containment/pre-post identity/SHA matches, protected mismatches=0, forbidden deltas=0, F1-F5 unstarted, original Task 5 blocked, Phase 1 blocked, acceptance pending; else exit 1 with non-acceptance diagnostics only. Evidence `%TEMP%\ace-v2-verifier-entrypoints-correction\<execution-id>\final-f4-scope-fidelity.md`.

## Commit strategy
- No task stages or commits files. The worktree is already dirty with unrelated owner changes, and this architecture workflow requires separate owner-controlled import/commit handling.
- The canonical repository plan is imported and owner-approved before execution; it is a protected authority, not one of the three correction-execution writes. If a later owner-authorized implementation commit is requested, include only `verify-widget.mjs`, the correction receipt and factual `TASK_CONTEXT.md` changes after all four final verifiers approve; proposed message: `fix(architecture): add amendment verifier entrypoints`. Never stage or commit the `.omo` source as repository authority.
- Rollback is path-specific and owner-authorized: validate the canonical repository plan with the trusted-local eight-step procedure and require its Todo 1 approved/imported SHA, restore only the pre-hash bytes of `verify-widget.mjs`, remove only the correction-created receipt, and append a factual `blocked` transition retaining `Correction overlay=docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment-verifier-entrypoints-correction.md` with its immutable SHA. Any authority discrepancy fails closed before rollback edits and creates no acceptance receipt. Never roll back, delete, rewrite or substitute the imported correction plan; never reset/clean/stash/broad-restore or rewrite approved/history artifacts.

## Success criteria
- `verify-widget.mjs` recognizes exactly seven suites: the unchanged `model-v2`/`runtime-v2` plus the five canonical amendment entrypoints, with deterministic suite-specific argument validation and no fallthrough.
- Existing suites pass from task-temporary outputs with the same statuses, assertion IDs/totals and mutations; no canonical old receipt is overwritten.
- F1-F5 handlers independently encode the approved criteria and output ownership, but no semantically complete amendment F-suite command or canonical amendment F receipt was produced during correction.
- F4/F5 future command authority is unambiguously retry-1; the approved amendment remains byte-identical and the failed initial snapshot is preserved and rejected as input.
- All failure probes are nonzero, deterministic, remove temporaries and prove byte-identical pre-existing outputs; F5 accepts only four distinct canonical regular receipts and writes aggregate only after complete approval.
- Authority inputs are checked cross-platform with ordinary `realpath`, canonical-root containment, regular pre-stat, one complete byte read plus SHA-256, stable post-stat `dev`/`ino`/`size`/`mtimeMs`, and expected-SHA equality; outside-root, directory, between-check mutation, hash mismatch and disappearance all fail closed without an acceptance receipt.
- The authority contract is explicitly limited to accidental-drift detection in a trusted local environment. It makes no protection claim for active filesystem attacks, intentional TOCTOU substitution or malicious reparse-point races, and introduces no descriptor-relative traversal, same-descriptor hashing, native helper, FFI or dependency.
- The only repository changes relative to Todo 1 are the three allow-listed paths; every unrelated dirty path and accepted/historical artifact is unchanged.
- The canonical imported correction plan at `docs/architecture-migration/plans/phase-0.5-model-driven-architecture-widget-v2-amendment-verifier-entrypoints-correction.md` is the sole authority, remains byte-identical to its owner-approved/imported/executed SHA, and is used by Todo 1, Todo 8, Final F1, Final F4, commit/rollback policy and `TASK_CONTEXT.md`; no required QA or workflow field treats `.omo/plans/...` as authoritative.
- The correction receipt ends PASS and `TASK_CONTEXT.md` truthfully records entrypoints available while amendment F1-F5 remain unstarted, original Task 5 and Phase 1 remain blocked, and phase result acceptance remains pending.
- Final correction verifiers F1-F4 all return unconditional APPROVE. This completes only the correction implementation; future semantic amendment F1-F5, original Task 5, owner acceptance, and Phase 1 remain separately gated.
