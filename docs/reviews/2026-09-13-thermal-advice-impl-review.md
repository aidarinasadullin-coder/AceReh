REVIEW_ID: R-2026-09-13-07
SUBJECT: дифф реализации плана thermal-advice (фазы A–C: ThermalAdviceService,
карточка «Рекомендации», хинты, UiSmoke) + соответствие исправленному плану
docs/plans/2026-09-13-thermal-advice-plan.md
REVIEWER: независимый read-only subagent, чистый контекст (general-purpose,
read-only по инструкции, 84 инструмента за проход, 2 воспроизведения полного
прогона)
DATE: 2026-09-13
RECEIPT: настоящий файл (полный отчёт ревьюера сохранён ниже)
VERDICT: APPROVE-WITH-EDITS
REASON: контент фаз A–C атаку выдержал — якоря точные (A1), сущности существуют
(A2), ripple чист (A3: 26 тестовых мест на опциональном ctor-параметре,
биндинги резолвятся, .smc-персистентность MeanTemperature даёт корректные
советы на загруженном проекте), R1–R6 не задеты (A4), физика и арифметика
советы/хинты/manual/show-me едины (A5). Ключевая находка P1 — процессная:
урок №32 не был внесён в rules-registry, RulesRegistryTests падали на полном
прогоне (прогон автора был сделан до append урока) — противоречие с правилом
«зелёное дерево» перед handover. Реализация после внесения находок и
перезапуска полного прогона.

## Находки и статусы

| # | Приоритет | Находка | Статус |
|---|---|---|---|
| 1 | P1 | Урок №32 не внесён в `docs/agents/rules-registry.md` → `RulesRegistryTests.Registry_OwnerRulesAndLessons_AllMentioned` детерминированно падает (заявление «2290 passed / 0 failed» на переданном дереве не воспроизводилось — прогон был сделан до append урока №32) | исправлено: строка `ARCH-AUTID-PEER` добавлена в реестр, полный прогон перезапущен до handover |
| 2 | P2 | Решение №8 (открытый owner-гейт) фактически присвоено реализацией: вариант A выбран по сигналу «делай» без явного выбора; §2 плана и show-me («open») противоречили шапке | исправлено: §2 плана и show-me синхронизированы — «A реализован по сигналу „делай“, подтверждение выбора — на приёмке G2 (откат к B тривиален)» |
| 3 | P2 | Поведение варианта A (скрытие «Рекомендуется: …» при активных советах, `ThermalViewModel.cs:323–329`) не покрыто ни одним тестом | исправлено: `ThermalAdviceViewModelTests` — советы активны → `SupplyTemperatureHint` пуст, `AdviceSupplyHint` показан; чистый результат → хинт возвращён, советы скрыты |
| 4 | P3 | §5 фаза C плана не синхронизирована с реализацией: обещан UiSmoke «отрицательная обратка → карточка», поставлен сценарий `DELTAT_MAX` (подача 90 °C) | исправлено: §5 уточнён с причиной (входная валидация «подача ≥ 2×T_средняя» не гарантирует `RETURN_NEGATIVE`; ΔT > 30 K при подаче 90 °C — гарантирован) |
| 5 | P3 | `ThermalAdviceGoToButton` дублируется в рантайме при двух активных советах (контракт уникальности проверяет только статический XAML) | отложено: безвредно в v1 (оба совета ведут на один шаг); v2 при разных `TargetStep` — суффикс по Id совета, зафиксировано в §8 плана |

## Подтверждено ревьюером (выборка с путями:строками)

- A1: ThermalResultValidator.cs:32–38/:48–53; ThermalCalculator.cs:411/469–478/481–484/533–537;
  MainViewModel.cs:123–127/:227–236; DI-вставка ServiceCollectionExtensions.cs:291.
- A2: NavigateToTargetCommand, SelectedMenuItem (публичный сеттер),
  StringToVisibilityConverter, FormCard.Hint/Button.Secondary, токены
  Brand.Red*/Brand.White/Brand.Gray.100 — существуют; хелперы UiSmoke — на месте.
- A3: 26 тестовых мест `new ThermalViewModel(…)` компилируются на опциональном
  параметре; DataContext-цепочка окна → ThermalView подтверждена
  (MainWindow.xaml.cs:85, :371); MeanTemperature персистится
  (ThermalPersistenceMapper.cs:94,230) — советы корректны на загруженном .smc;
  на раннем возврате калькулятора ложные советы математически невозможны.
- A4: writers R2/WI-1..7, R3/R4/R5, R6 — не задеты; ArchitectureRulesTests зелёные.
- A5: ориентиры 29,2 (=2×T_средняя) и 29,6 (=T_средняя+15) точны; направление
  «подача ↓ / шаг ↑» едино в сервисе/хинте/manual 6.8/show-me; тест
  эквивалентности с валидатором на трёх точках зелёный.
- A6: XAML ↔ контракт ↔ UiSmoke ↔ show-me согласованы; запятая — канон AppCulture
  (locale-тест с en-US-подменой).
- A8: чек плана R-2026-09-13-06 и quality-log на месте; lessons.md №32 —
  чистый append; чек настоящего ревью создаётся по итогам.

---

## Вектора атаки (канонический блок промпта ревью — урок №26)

```text
Роль: независимый read-only ревьюер, чистый контекст, не автор изменения.
Задача — АТАКА: найди, чем план/дифф провалится при реализации, а не
подтверди корректность. Каждый вектор — отдельный проход:
A1. Якоря «файл:строка» — каждый открыть и сверить по коду.
A2. Названные в плане поля/методы/типы — существуют ли в заявленных
    источниках (сигнатуры, снапшоты, XAML, csproj).
A3. Ripple: ctor-параметры, места конструирования в тестах, привязки
    XAML, деплой-файлы, AutomationId-контракты.
A4. Архитектура: сканы R1–R6, списки санкционированных writers,
    wire-контракт .smc.
A5. Арифметика: суммы и числа плана и show-me сходятся между собой.
A6. show-me ↔ план: сущности, подписи, реальные строки UI; токены —
    канон Tokens.Colors.xaml.
A7. Гейты: не присвоил ли план owner-решение; спорные решения —
    два варианта (A/B) с явным выбором владельца.
A8. Процесс: чек ревью существует; записи quality-log последних
    фаз на месте.
Вердикт: APPROVE | APPROVE-WITH-EDITS | REJECT + находки P0–P3 со
статусами (исправлено в диффе / отложено / отклонено владельцем).
Чек: docs/reviews/YYYY-MM-DD-<topic>-review.md.
```
