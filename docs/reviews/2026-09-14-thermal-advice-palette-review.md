REVIEW_ID: R-2026-09-14-01
SUBJECT: дифф палитры B + удаление кнопок перехода (план thermal-advice,
послеотзывные решения владельца 2026-09-14): токен Status.Warning.Background,
карточка/бейджи ThermalView, severity Warning, NavigateToTarget удалён,
документация и show-me
REVIEWER: независимый read-only subagent, чистый контекст (general-purpose,
read-only по инструкции, 55 инструмента за проход, trx-сверка прогона)
DATE: 2026-09-14
RECEIPT: настоящий файл (полный отчёт ревьюера сохранён ниже)
VERDICT: APPROVE-WITH-EDITS
REASON: P0/P1 нет. Код консистентен, XAML структурно целен, инварианты
R1–R6 и wire .smc не задеты, гигиена токенов соблюдена (новый тинт —
токеном, литеральных HEX во ThermalView нет), арифметика тинта и советов
сходится, owner-решения (палитра B, удаление кнопок) зафиксированы как
owner-решения. Две P2 — гигиена следов удалённой кнопки и зависимость
канона от untracked файла чужой сессии; две P3 — остаточные формулировки.
Все находки внесены до коммита.

## Находки и статусы

| # | Приоритет | Находка | Статус |
|---|---|---|---|
| 1 | P2 | Имя smoke-теста `ExcessiveSupply90_ShowsAdviceCard_WithGoToButton` противоречит телу (пинует отсутствие кнопки) — грепающий увидит зелёный тест и решит, что кнопка есть | исправлено: переименован в `…_WithoutGoToButton` |
| 2 | P2 | Комментарий канона в Tokens.Colors.xaml ссылается на untracked `tokens.json` чужой сессии — при раздельном коммите ссылка канона укажет на файл вне истории; принятие tokens.json как источника апп-токенов нигде не зарегистрировано | исправлено: первичным источником назван tracked `docs/design/brandbook-2026-application.md` §2.3, tokens.json — вторичный (§4 плана синхронизирован) |
| 3 | P3 | Остаточные упоминания кнопок без пометки «отменено»: план §3 UI-буллет, §4 («Кнопки перехода — teal», колонка «Переход | Thermal»), §5 фаза C, §8 (2 строки); show-me фаза C «+ item-кнопки» | исправлено: все строки помечены историческими/обновлены, колонка → «Переход (v2)» |
| 4 | P3 | Palette-show-me фиксирует «B с кнопками» — расхождение с кодом при сверке артефакта с планом (урок №27) | исправлено: в блок решения добавлено примечание об удалении кнопок в тот же день (§13) |
| 5 | P3 | Мёртвый CSS-класс `.sev.error` в регенерированном show-me | исправлено: удалён |

## Подтверждено ревьюером (выборка с путями:строками)

- A1: ThermalAdviceCard на TextBlock ровно один (ThermalView.xaml:404–407,
  контракт ThermalAutomationIdSelectorContractTests.cs:87 выполняется);
  токен/кисть Tokens.Colors.xaml:50/:81; бейджи :153–162/:271–280;
  якоря ThermalResultValidator.cs:32–38/:48–53 точны.
- A2: все именованные brush'и и стили существуют (Brand.White/Gray.300/
  Gray.800/Black, Status.Warning[.Background].Brush, FormCard.Hint с
  TextWrapping, Button.Secondary жив для ThermalReset); цитата
  tokens.json дословная.
- A3: NavigateToTargetCommand/ThermalAdviceGoToButton в живом коде
  отсутствуют (проверено грепом); XML сбалансирован; CornerRadius полосы
  7 = 8−1.
- A4: AdviceList derived (ThermalViewModel.cs:336–341), ThermalAdvice не
  персистится — .smc нетронут; writers не менялись; ViewTokenHygieneTests —
  литеральных HEX нет, allowlist (0,0) держится.
- A5: 0x1F=31/255=12,16% ≈ 12% (Error 0x47≈28% — комментарий точен);
  ориентир T_средняя+7,5 одинаков в сервисе/плане/руководстве/show-me;
  числа моков сходятся.
- A6: карточка/бейджи/severity описаны одинаково в коде, плане, show-me;
  goto-чипы удалены полностью (после P3-1/P3-2 — и остатки).
- A7: палитра B и удаление кнопок — owner-решения с датами; рекомендация
  агента отделена от решения; v2 «Применить ориентир» — кандидат без
  реализации.
- A8: lessons/quality-log append-only не нарушены; чек данного ревью
  создаётся по итогам; конфликтов с чужой правкой rules-registry нет.

---

## Вектора атаки (канонический блок промпта ревью — урок №26)

```text
Роль: независимый read-only ревьюер, чистый контекст, не автор изменения.
Задача — АТАКА: найди, чем план/дифф провалится при реализации, а не
подтверди корректность. Каждый вектор — отдельный проход:
A1. Якоря «файл:строка» — каждый открыть и сверить по коду.
A2. Названные в плане поля/методы/типы — существуют ли в заявленных
    источниках (сигнатуры, снапшоты, XAML, csproj).
A3. Ripple: ctor-параметры, места конструирования в тестах, привязки
    XAML, деплой-файлы, AutomationId-контракты.
A4. Архитектура: сканы R1–R6, списки санкционированных writers,
    wire-контракт .smc.
A5. Арифметика: суммы и числа плана и show-me сходятся между собой.
A6. show-me ↔ план: сущности, подписи, реальные строки UI; токены —
    канон Tokens.Colors.xaml.
A7. Гейты: не присвоил ли план owner-решение; спорные решения —
    два варианта (A/B) с явным выбором владельца.
A8. Процесс: чек ревью существует; записи quality-log последних
    фаз на месте.
Вердикт: APPROVE | APPROVE-WITH-EDITS | REJECT + находки P0–P3 со
статусами (исправлено в диффе / отложено / отклонено владельцем).
Чек: docs/reviews/YYYY-MM-DD-<topic>-review.md.
```
