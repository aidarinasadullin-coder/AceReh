REVIEW_ID: R-2026-09-13-06
SUBJECT: план docs/plans/2026-09-13-thermal-advice-plan.md (советы теплового
расчёта — ThermalAdviceService + карточка «Рекомендации», идея №2) + show-me
docs/plans/show-me-thermal-advice.html
REVIEWER: независимый read-only subagent, чистый контекст (general-purpose,
read-only по инструкции, 33 инструмента за проход)
DATE: 2026-09-13
RECEIPT: настоящий файл (полный отчёт ревьюера сохранён ниже)
VERDICT: REJECT
REASON: каркас плана (derived-слой, сервис, DI, фазы, гейты) архитектурно
чист — R1–R6, writers, wire .smc не задеты (проход A4), но содержание
каталога советов — ядро плана — не проходит атаку: один из трёх советов
построен на несуществующей в коде величине (P0 — «расчётной t_пов» нет в
ThermalCalculationResult: t_пов — вход расчёта, ThermalCalculator.cs:411),
два других дают лечение в обратную сторону (P1×2 — арифметика
T_обратка = 2×T_средняя − T_подача, ΔT = 2×(T_подача − T_средняя)),
заявленная навигация использует свойство с приватным сеттером (P1).
Выбор владельца получен по show-me с неверными советами — после внесения
находок: регенерация show-me (урок №27) и повторный G1. Реализация —
после повторного owner-signal.

## Находки и статусы

| # | Приоритет | Находка | Статус |
|---|---|---|---|
| 1 | P0 | Совет `SURFACE_ABOVE_TARGET` нереализуем: «расчётной t_пов» в модели не существует (`ThermalCalculator.cs:411` — surfaceTemp = (int)inputs.Mode — вход; `ThermalCalculationResult.cs:57–82` — свойства нет); show-me показывал 8.2 °C > MaxSurfaceTemperature=7 | исправлено в этом же диффе: совет исключён из каталога v1, §1/§2/§4 скорректированы, show-me регенерирован |
| 2 | P1 | `RETURN_NEGATIVE`: направление лечения перевёрнуто — «увеличьте подачу до ≈29.2» при подаче 50 (рост подачи углубляет отрицательную обратку; текст унаследован от старого нереализованного предложения Планируемых_изменений п.1) | исправлено: «уменьшите подачу до ≈2×T_средняя или увеличьте шаг» (§4) |
| 3 | P1 | `DELTAT_MAX`: «увеличьте подачу» и «уменьшите нагрузку» ведут в обратную сторону (ΔT = 2×(T_подача − T_средняя); excess растёт по q → T_средняя ↓ → ΔT ↑), «проверьте гликоль» фиктивен (гликоль только в расходе, ThermalCalculator.cs:533–537) | исправлено: «уменьшите подачу (≈T_средняя + 15 K) или увеличьте шаг», гликоль убран (§4) |
| 4 | P1 | Навигация через `CurrentNavigationTarget` невозможна — сеттер приватный (`MainViewModel.cs:123–127`); санкционированный путь — `SelectedMenuItem` (прецедент HeaderCalculate, MainViewModel.cs:227–236); обвязка отсутствовала в файл-листе | исправлено: механизм «поиск MenuItem по Target → SelectedMenuItem», обвязка в файл-листе фазы B (§3, §5) |
| 5 | P2 | Show-me противоречил плану в направлении рычага «шаг» («уменьшите шаг» vs план «увеличьте шаг») | исправлено: регенерация из исправленного плана |
| 6 | P2 | Счёт советов: вход «подача 50 / средняя 14.6» даёт два нарушения (обратка −20.8 и ΔT 70.8 > 30), show-me показывал один («Рекомендации (1)») | исправлено: мок и симуляции показывают два совета; тест-таблица фазы A согласована |
| 7 | P2 | Ripple ctor: ~30 тестовых мест `new ThermalViewModel(…)` — внедрение сервиса сломает все; стратегия не описана | исправлено: опциональный параметр `IThermalAdviceService? adviceService = null` по прецеденту `coordinator = null` (ThermalViewModel.cs:399) (§3) |
| 8 | P2 | `ThermalAdviceItem_N`/`ThermalAdviceGoTo_N` непинниуемые XAML-контрактом — item-советы генерируются ItemsControl в рантайме, парсер исходников их не увидит | исправлено: в контракт только статическая `ThermalAdviceCard`; item-ID — UiSmoke фазы C (§5) |
| 9 | P2 | Конфликт с постоянным хинтом «Рекомендуется: … (для ΔT ≈ 15 К)» (`ThermalViewModel.cs:310–321`) не определён — две конкурирующие цифры под одним полем; show-me существующий хинт не показывал | отложено: решение №8 — открытый owner-гейт при повторном G1 (§2, §8); show-me теперь показывает оба хинта |
| 10 | P3 | Тест эквивалентности с валидатором: ветка «ΔT ≤ 0» (ранний возврат, ThermalCalculator.cs:469–478) без парного совета | исправлено: оговорка скоупа сверки в фазе A (§5) |
| 11 | P3 | Show-me в точечной десятичной записи против канона ru-RU (запятая, пин RussianNumberCultureTests); якорь PowerSummary смещён (328–332, не 333); зависимость фазы B от surface-temperature устарела (реализована в дереве: Manual-режимы OperatingMode.cs:38–57, урок №24) | исправлено: запятые при регенерации; якоря обновлены; зависимость снята с обоснованием (§3, §5, §11 плана) |

## Подтверждено ревьюером (выборка с путями:строками)

- A1: якоря ThermalResultValidator.cs:32–38 и :48–53 — точные;
  ServiceCollectionExtensions.cs:290 — точная строка регистрации;
  ThermalView.xaml:73 — дословное совпадение заголовка мока.
- A2: ValidationConstants.MaxDeltaT=30 (ValidationConstants.cs:96),
  NavigationTarget, AppCulture.Culture, FormCard.Hint, контракт-тест —
  существуют; «расчётная t_пов» — не существует (P0).
- A4: чисто — writers (AssertSanctioned R2/WI-1..7) не затронуты,
  ThermalAdviceService не зависит от VM (R4), VM-свойство-проекция (R3),
  .smc (R6) не трогается, «ADR не требуется» корректно.
- A5: арифметика чисел сходится (2×14.6=29.2; −20.8; 70.8), направления
  лечения — нет (P1×2).
- A6: 13 hex-значений show-me совпали с Tokens.Colors.xaml 1:1; находки —
  рычаг «шаг», скрытый хинт, точка вместо запятой, несуществующие 8.2 °C.
- A7: 7 решений A/B с выбором владельца на месте; присвоения owner-решений
  нет; но сам выбор получен по show-me с неверными советами → повторный G1.
- A8: чек создаётся по итогам этого ревью; quality-log без пропусков;
  уроки №26–28 на месте.

---

## Вектора атаки (канонический блок промпта ревью — урок №26)

```text
Роль: независимый read-only ревьюер, чистый контекст, не автор изменения.
Задача — АТАКА: найди, чем план/дифф провалится при реализации, а не
подтверди корректность. Каждый вектор — отдельный проход:
A1. Якоря «файл:строка» — каждый открыть и сверить по коду.
A2. Названные в плане поля/методы/типы — существуют ли в заявленных
    источниках (сигнатуры, снапшоты, XAML, csproj).
A3. Ripple: ctor-параметры, места конструирования в тестах, привязки
    XAML, деплой-файлы, AutomationId-контракты.
A4. Архитектура: сканы R1–R6, списки санкционированных writers,
    wire-контракт .smc.
A5. Арифметика: суммы и числа плана и show-me сходятся между собой.
A6. show-me ↔ план: сущности, подписи, реальные строки UI; токены —
    канон Tokens.Colors.xaml.
A7. Гейты: не присвоил ли план owner-решение; спорные решения —
    два варианта (A/B) с явным выбором владельца.
A8. Процесс: чек ревью существует; записи quality-log последних
    фаз на месте.
Вердикт: APPROVE | APPROVE-WITH-EDITS | REJECT + находки P0–P3 со
статусами (исправлено в диффе / отложено / отклонено владельцем).
Чек: docs/reviews/YYYY-MM-DD-<topic>-review.md.
```
