# Review Receipt — R-2026-09-12-01

```text
REVIEW_ID: R-2026-09-12-01
SUBJECT: план docs/plans/2026-09-12-process-gates-plan.md
         + show-me docs/plans/show-me-process-gates.html
         («Процессные гейты уровня 3: CI, реестр правил, ревью-чеки»)
REVIEWER: независимый read-only subagent, чистый контекст (Explore)
DATE: 2026-09-12
RECEIPT: настоящий файл (полный отчёт ревьюера сохранён в «Находках»)
VERDICT: APPROVE-WITH-EDITS
REASON: план согласуется с фактическим состоянием репозитория (версии,
        тесты, якоря скан-тестов, формат ADR-заголовков — проверены по коду);
        после внесения находок P1–P3 в этот же дифф дизайн не меняется,
        owner-гейты сохранены, скоуп не расширен. Реализация — после
        owner-signal «делай» (гейт G1).
```

## Подтверждено ревьюером по коду

- Версия 1.2.0 сходится во всех заявленных решением 7 местах
  (`src/SnowMeltingCalculator.csproj:11`; `installer/SnowMeltingCalculator.iss:4,18`;
  `INSTALL.md:12,80` — ровно 2 вхождения имени сетапа; `README.md:71`).
- `### ADR-\d+` матчит ровно 14 заголовков ADR-001…014 в
  `docs/architecture/README.md` — проверка B реализуема.
- `.github/` и `global.json` отсутствуют; локальный SDK 8.0.418; remote — GitHub.
- Все упомянутые планом тесты существуют (`ReactiveSubscriptionLifecycleTests`,
  `ViewTokenHygieneTests`, `ThermalAutomationIdSelectorContractTests`,
  `ProjectSnapshotFactoryTests`, `ProjectSnapshotContractTests`).
- Категория `UiSmoke` на `UiSmokeFixtureBase.cs:16`, наследуется всеми
  смоук-фикстурами — фильтр `Category!=UiSmoke` работает.
- Оба прецедента обнаружения корня репо пригодны на CI-раннере:
  `ArchitectureRulesTests.cs:31-40` (walk-up к .sln) и
  `ThermalAutomationIdSelectorContractTests.cs:26-29` (относительный путь от assembly).
- Скелет yml корректен (checkout@v4, setup-dotnet@v4, 8.0.x, windows-latest,
  NUnit-фильтры; namespace `SnowMeltingCalculator.Tests.Architecture`);
  NuGet-пакеты публичные; `SelfContained+win-x64` в src csproj сборке не мешает.
- Культура — не риск: locale-тесты сами пинят ru-RU через `AppCulture`
  (`RussianNumberCultureTests.cs:22-25`), на en-US раннере зелёные.

## Находки и статусы

| # | Приоритет | Находка | Статус |
|---|---|---|---|
| 1 | P1 | show-me: `classList.add('')` (пустой токен DOMTokenList) бросал `SyntaxError`; стартовый `setMode('after')` прерывал скрипт до инициализации SIMS — интерактивность артефакта мертва (урок №21: владелец открыл бы нерабочую страницу) | **исправлено в этом же диффе** (guard `if(c.cls)`, инициализация перенесена в конец скрипта) |
| 2 | P1 | план §3: утверждение «`TreatWarningsAsErrors` уже в тестовом csproj» фактически неверно — свойства нет нигде в репо (в csproj только комментарий) | **исправлено в этом же диффе** (§3: фактическое состояние + порог предупреждений — владельческое решение вне скоупа) |
| 3 | P1 | проверка C верификатора покрывала только 2 секции lessons.md из обещанных §4 (заголовки без маркера «правило владельца» не подпадали) — реестр мог протухнуть молча | **исправлено в этом же диффе** (якорь расширен: «правило владельца» / `Урок №\d+` / номера в заголовках; риск C поднят до средней) |
| 4 | P2 | VersionSyncTests из 5 значений покрывал не весь чек-лист урока №20 (комментарий .iss, подвал INSTALL.md, секция CHANGELOG оставались вне) | **исправлено в этом же диффе** (решение 7 и §4: 8 мест) |
| 5 | P2 | шапка плана опережала факт («ревью пройдено» при идущем ревью), §11 пуст | **исправлено в этом же диффе** (шапка по факту, §11 заполнен, настоящий чек создан) |
| 6 | P2 | не зафиксирована грамматика колонки «Как проверить» — reflection-резолв проверки A недетерминирован при прозе в ячейке | **исправлено в этом же диффе** (§4: при `test` — точные имена через запятую, без прозы) |
| 7 | P3 | show-me: `--red-pale`/`--teal-pale` не из канона `src/Themes/Tokens.Colors.xaml` | **исправлено в этом же диффе** (канон: `Brand.Red.Tint #FCC2B8`, `Brand.Teal.Pale #95DDD3`) |
| 8 | P3 | show-me: маркер «сейчас · 2+» не имел опоры в плане; карточка чека без DATE/RECEIPT | **исправлено в этом же диффе** (§1 дополнен позицией «2+»; карточка дополнена) |
| 9 | P3 | правка AGENTS.md должна коммититься отдельно от артефактов фазы; процессные находки — вне ADR-журнала | **исправлено в этом же диффе** (решение 10, §7) |
| 10 | P3 | инвентаризация §4 без трёх дешёвых машинно-проверяемых правил (культура биндинга, «workspace/publish вне git», ratchet-бейзлайн) | **исправлено в этом же диффе** (§4 дополнен; объём 20–25 строк) |
| 11 | P3 | двойной прогон Architecture-набора в CI — приемлемо, но отметить | **исправлено в этом же диффе** (§3: осознанная цена, решение после первого замера) |

## Замечание по процессу

Настоящий чек — **dry-run** контракта ревью (решение 9 плана): ревью
выполнено до включения фазы C, формат применён к самому плану. Правки по
находкам 1–11 внесены автором плана после ревью; повторный проход не
требовался (APPROVE-WITH-EDITS, дизайн не менялся).
