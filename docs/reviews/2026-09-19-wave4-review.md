REVIEW_ID: R-2026-09-19-01
SUBJECT: незакоммиченный дифф волны 4 «наблюдаемость» (план docs/plans/2026-09-18-hardening-roadmap-plan.md §5, D2) — RollingFileLogger + фасад AppLog + перепись 80 catch + SilentCatchScanTests; полный отчёт ревьюера ниже
REVIEWER: независимый read-only subagent, чистый контекст (Explore)
DATE: 2026-09-19
RECEIPT: настоящий файл (полный отчёт ревьюера сохранён ниже)
VERDICT: APPROVE-WITH-EDITS
REASON: поведение catch не изменено (18 мест выборочно — statements/return-пути
целы), покрытие всех 80 catch подтверждено, ротация/инъекция/скан-тест
работают. Минимальный набор правок до коммита (№1, 2, 3, 6, 7, 9) внесён
автором и проверен прогоном; №4, №5 — отложены владельцем с фиксацией в
плане; №8, №10–№13 — приняты/зафиксированы. Гейт-отступление: ревью
проведено по диффу после имплементации, а не «до» (инвентарь 80 catch
требовал кода для ревизии) — компенсировано полнотой прохода A1–A8 и
внесением всех P1-правок до коммита.

## Находки и статусы

| # | Приоритет | Находка | Статус |
|---|-----------|---------|--------|
| 1 | P1 | Шаблонные заглушки «.method» вместо имени метода — 5 мест (App.xaml.cs:93, ThermalCalculator:546, MaterialEditorViewModel:324, ConstructionViewModel:1011, CircuitsViewModel:717) | исправлено: реальные имена методов (OnStartup, CalculatePowerDown, SaveAsync, LoadConstruction, CalculateCollector) |
| 2 | P1 | Ни одного AppLog.Error (67 Warn / 0 Error), ProjectFileService save/load — Warn вопреки плану §5; суффикс «: обработано» семантически ложный | исправлено: ProjectFileService ×5 → Error(message, ex); суффикс «: обработано» снят globally (67) |
| 3 | P1 | Тесты/CI пишут в реальный %LOCALAPPDATA% (18 call-site AddApplicationServices без SNOWCALC_LOG_DIR; CI гоняет suite дважды) | исправлено: tests/GlobalTestSetup.cs ([SetUpFixture] SNOWCALC_LOG_DIR → %TEMP%\snowcalc-test-logs) |
| 4 | P2 | «Разовое предупреждение в UI» для справочников не сделано (только log) | отложено владельцем: канал UI-предупреждения — точка дизайна; логирование сделано |
| 5 | P2 | «29 Debug.WriteLine → log» вне catch не выполнено; в catch — продублированы | отложено: дубль в catch безвреден; вне catch — по мере касания |
| 6 | P2 | OperationCanceledException (отмены поиска) — Warn на каждый ввод | исправлено: отмены не логируются (норма потока); скан-тест исключает OCE-кэтчи |
| 7 | P2 | Инстансный lock при двух копиях приложения | исправлено: static lock (in-process); кросс-процессные строки при гонке глотаются (задокументировано) |
| 8 | P2 | Обходы скан-теста (комментарии в теле, скобки в интерполяциях) | отложено follow-up (rescan чист, ложных красных нет) |
| 9 | P3 | AppLog.Error дублирует исключение дважды в строке | исправлено: фасад передаёт exception без Format-хвоста |
| 10 | P3 | Скрипт снял UTF-8 BOM в 6 файлах; стиль скобок Allman → K&R в ~44 местах | принято осознанно (UTF-8 без BOM валиден, стиль единообразен) — шум диффа |
| 11 | P3 | AppLog.Register(null!) в тестах — глобальное состояние без Unregister | принято: NUnit однопоточный, зависимостей нет |
| 12 | P3 | Directory.CreateDirectory на каждую запись; fallback глотает всё | принято (задокументировано в RollingFileLogger) |
| 13 | P3 | Несвязанный мусор в дереве (docs/plans/2026-09-19-* от параллельной сессии) | учтено: коммит волны — явным pathspec, мусор не вошёл |

## Подтверждено ревьюером (выборка)

- A1/A2: ~18 hunks вскрыто — вставки синтаксически корректны, переменные
  существуют/введены безопасно, потери statements нет, дублей записи нет.
- A3: ротация корректна (maxFiles=3, старейший до сдвига, overwrite валиден);
  SNOWCALC_LOG_DIR-шов работает; RollingFileLoggerTests ротации/многопоточности/
  недоступного корня адекватны.
- A4: SrcRoot (+5 уровней) верен для Debug и CI; /bin/ /obj/ исключены;
  RollingFileLogger — единственный allowlist.
- A5: поведение не изменено (только записи на диск; отмены — находка 6).
- A6: AppLog.Register — первым в AddApplicationServices, до undo-дневника
  (план :291) ✓.
- A7: 7 комментарий-only catch закрыты логом ✓; пункт «29 Debug.WriteLine» —
  находка 5; «UI-предупреждение справочников» — находка 4.
- A8: новые файлы в правильных каталогах; docs диффом не тронут.

## Вектора атаки (канонический блок — урок №26)

Канонические вектора A1–A8 из docs/agents/review-receipt-template.md
(якоря файл:строка; скрытые поломки автоскрипта; дизайн логгера: гонки/
ротация/фолбэк/шов; обходы скан-теста; неизменность поведения; DI/порядок;
гейты плана; процесс) — применены к диффу волны 4.
