# Review Receipt — R-2026-09-13-04

```text
REVIEW_ID: R-2026-09-13-04
SUBJECT: актуализация 2026-09-13 (§12) процессного плана
         docs/plans/2026-09-12-process-gates-plan.md
         + синхронизированный show-me-process-gates.html
REVIEWER: независимый read-only subagent, чистый контекст (Explore)
DATE: 2026-09-13
RECEIPT: настоящий файл (полный отчёт ревьюера)
VERDICT: APPROVE-WITH-EDITS
REASON: Дельта фактически точна: коммиты 79b6145/8d70d27 содержат ровно то,
        что приписано; девять мест версии сходятся (1+3+3+1+1); show-me
        синхронизирован, JS жив, owner-гейты не присвоены. Одна P2 —
        призрачные REVIEW_ID в §12 п.2 (тот же класс, что уже ловили в
        R-2026-09-13-03) — и четыре P3 (недокументированные отклонения
        ci.yml, протухший «2237 passed», нереализуемость литерал-сравнения
        двух макро-мест .iss, мусор в дереве под коммит).
```

## Находки и статусы

| # | Приоритет | Находка | Статус |
|---|---|---|---|
| 1 | P2 | §12 п.2: «узаконен чеками R-2026-09-12-01, -01, -02, -03» — шорткат даёт дубль `R-2026-09-12-01` и несуществующие `R-2026-09-12-02/-03`; фактический набор — `R-2026-09-12-01` + `R-2026-09-13-01/-02/-03`. Тот же класс баги, что P2 №2 чека R-2026-09-13-03 (ghost-ID); план сам был жертвой этого класса | **исправлено в этом же диффе** (§12 п.2 — перечень полностью); системная причина записана уроком №29 в `lessons.md` |
| 2 | P3 | Дельта молчит об отклонениях ci.yml от скелета §3: `timeout-minutes: 45` (.github/workflows/ci.yml:14) и `--nologo` (:29, :33) не упомянуты в §12 п.1 — сверка §3 ↔ закоммиченный файл покажет необъяснённый дрейф | **исправлено в этом же диффе** (§12 п.1: «файл сверх решений добавляет timeout-minutes: 45 и --nologo») |
| 3 | P3 | §2 реш.7/§4/§12 п.4: «расхождение любого места — падение» буквально нереализуемо: 2 из 9 мест .iss не содержат литерала версии — `#define MyAppVersion` вычисляется через `GetVersionNumbersString` (installer/SnowMeltingCalculator.iss:16–22), `OutputBaseFilename` подставляет макрос (:33). Литерал-сравнение по ним всегда расходится с каноном | **исправлено в этом же диффе** (§4: два макро-места проверяются структурно — шаблон строки и шапка-комментарий, остальные семь — точным совпадением) |
| 4 | P3 | §10: «галочка CI на коммите вместо строки „2237 passed“» — протухшее число (фактический полный набор на 2026-09-13 — 2263 passed / 0 failed / 1 skipped, коммит 79b6145) | **исправлено в этом же диффе** (обобщено: «вместо строки „N passed“») |
| 5 | P3 | В некоммиченном дереве лежит несвязанный мусор, в т.ч. бинарный untracked `tests/SnowMeltingCalculator.Tests/Fixtures/Спецификация_T19-REGRESSION-001_20260913.xlsx` — обобщающий `git add .` по сигналу G1 затянет бинарник в репо | **отложено — решение владельца**: §12 п.7 дополнен правилом адресного `git add`; судьбу xlsx-фикстуры (коммит/удаление/игнор) решает владелец до G1 |

## Подтверждено ревьюером (выборка с путями:строками)

- **A1 (коммиты).** `git show --stat 79b6145`: ровно один файл
  `.github/workflows/ci.yml` (+34), чужих изменений нет; сообщение совпадает
  с §12 п.1. `git show --stat 8d70d27`: ровно `AGENTS.md` (+4 = один пункт
  из четырёх строк); текст пункта (AGENTS.md:55–58) дословно покрывает
  заявленное: CI на каждый push, зелёный прогон — условие готовности
  handover, красный master блокирует старт новых фаз.
- **A2 (ci.yml ↔ решения 1–4).** windows-latest (:13), SDK 8.0.x (:20),
  триггеры push→master + pull_request (:6–9), ступень 1 Architecture fast
  feedback (:28–30) → ступень 2 полный без UiSmoke (:32–34); `global.json`
  отсутствует — как решено (реш.1). Отклонения — только находка №2.
- **A2 (якорь категории).** `UiSmokeFixtureBase.cs:16` —
  `[Category("UiSmoke")]`, якорь реш.2 точен.
- **A2 (девять мест версии — фактический подсчёт, 1+3+3+1+1=9
  подтверждён).** csproj: `src/SnowMeltingCalculator.csproj:11`
  `<Version>1.5.0</Version>`; .iss ×3: шапочный комментарий «v1.5.0»
  (:13), `#define MyAppVersion` (:18/:20 в #if/#else), `OutputBaseFilename`
  (:33); INSTALL.md ×3: имя сетапа дважды (:12, :101) + подвал (:169);
  README.md подвал; CHANGELOG.md `## [1.5.0]` (:3). Provenance девятки —
  находка №4 в R-2026-09-13-02 (excel-review:28) с той же арифметикой.
- **A2 (уроки №22–28).** Все существуют (lessons.md:443, :476, :489, :506,
  :521, :539, :551); типы в §4 расставлены верно: №22-биндинг —
  «тест сюда не [ловит]» (lessons.md:452–454) → review-step; №23 — smoke
  владельцем → review-step; №24/№25 — именованные пин-тесты → test;
  №26–28 — review-step.
- **A3 (ripple).** R-2026-09-13-04 свободен (в docs/reviews/ ровно четыре
  чека); имя файла по конвенции; ссылки валидны: solo-loop план существует,
  его §3.1 — блок A1–A8 (:66–99), §6 — оговорка про enum (:185–187);
  quality-log.md содержит ровно четыре записи.
- **A4 (архитектура).** Дельта = 2 docs-файла; src/, tests/, `.smc`, R1–R6,
  writers не затронуты; дерево некоммичено до G1. Тест-имена из примеров
  реестра существуют (`R1_ProjectSession_IsAggregateRoot_WithFourExplicitSlices`
  — ArchitectureRulesTests.cs:84; `RussianNumberCultureTests`,
  `ViewTokenHygieneTests`, `ReactiveSubscriptionLifecycleTests`,
  `ProjectSnapshotFactoryTests`, `ProjectSnapshotContractTests` — найдены
  grep'ом).
- **A5 (арифметика).** Девять мест сходятся; чеков четыре; оценка «28–32
  строки» реалистична: построчный минимум инвентаризации §4 ≈ 29–31.
  Проверка C верификатора: якорные заголовки lessons.md — 2 «правило
  владельца» (:337, :371) + «Урок №21…№28» (:407–551) — все покрыты
  инвентаризацией §4; заголовков с «Урок №N» вне инвентаризации нет
  (№18 упомянут сверх требуемого — безвредно).
- **A6 (show-me ↔ план).** Кнопки/колонки «Сейчас · 2026-09-13» /
  «После · полный план»; бейджи «актуализация 2026-09-13 · §12», «Фаза A:
  внедрена · 79b6145», «Остаток: фаза B + хвост C»; карточки с статусами;
  симуляция 0 — девять мест версии = 1+3+3+1+1 плана; симуляция 2 —
  template + вектора A1–A8; шкала обновлена; футер G1/G2/G3 соответствует
  §6. Протухших фраз нет (grep «2237 / CI нет / BLOCKED» — 0). JS жив и
  безопасен (`setMode`/`runSim`/`clearSim`, guard от пустого класса,
  `setMode('after')` при загрузке); самодостаточен. Косметика вне находок:
  CSS-класс `.ghost` после дельты мёртв — ни один узел его не получает.
- **A7 (гейты).** G1/G2/G3 — решения владельца; дельта реально некоммичена;
  «gh в системе недоступен» — проверено (в PATH отсутствует).
- **A8 (процесс).** Нумерация цела (§1–§12 + §3.1); протухших формулировок
  не осталось («.github/» — только с оговоркой о внедрении; «BLOCKED» —
  единственное упоминание, как выведенный из употребления; «восемь» —
  только как «девять, не восемь» и историческая ссылка §11); шапка
  согласована с §12.
