# Задача: Исправить удельные потери давления

## Метаданные

| Параметр | Значение |
|----------|----------|
| **ID** | task_units_2 |
| **Модуль** | Hydraulics |
| **Приоритет** | P1 (Критичный) |
| **Статус** | Ожидает |
| **Зависимости** | — |
| **Юзер-кейс** | UC-2 |

---

## 1. Цель задачи

Исправить расчёт удельных потерь давления в методе `CalculateAtTemperature` — использовать плотность в **г/см³** вместо **кг/м³**.

---

## 2. Связь с юзер-кейсами

### UC-2: Расчёт удельных потерь давления

**Проблема**: Формула использует плотность в кг/м³, но требует г/см³. Результат в 1000 раз больше.

**Решение**: Конвертировать плотность: `density_g_cm3 = glycolProps.Density / 1000.0`

---

## 3. Описание изменений

### 3.1. Файл: `src/Services/Hydraulics/CircuitsCalculator.cs`

#### 3.1.1. Метод: `CalculateAtTemperature` (удельные потери)

**Строки**: 200-203

**Текущий код**:
```csharp
// Удельные потери: R = 10000 × (v² × ρ × λ) / (2 × d_inner) × 100
double pressureLossPerMeter = 10000 * Math.Pow(velocity, 2) * glycolProps.Density * frictionFactor
    / (2 * innerDiameter) * 100;
```

**Требуемый код**:
```csharp
// Удельные потери: R = 10000 × (v² × ρ × λ) / (2 × d_inner) × 100
// Важно: ρ должно быть в г/см³, glycolProps.Density в кг/м³
double density_g_cm3 = glycolProps.Density / 1000.0;
double pressureLossPerMeter = 10000 * Math.Pow(velocity, 2) * density_g_cm3 * frictionFactor
    / (2 * innerDiameter) * 100;
```

#### 3.1.2. XML-комментарий

**Обновить комментарий** (строки 143-156):

```csharp
/// <remarks>
/// Рассчитывает:
/// - Скорость потока v
/// - Число Рейнольдса Re
/// - Режим течения (ламинарный/переходный/турбулентный)
/// - Коэффициент трения λ
/// - Удельные потери R (Па/м)
/// - Потери в трубе контура Δp_HK (Па)
/// - Потери в трубе подводки Δp_Zul (Па)
/// - Потери в вентиле Δp_Vent (Па)
/// - Суммарные потери Δp_total (Па)
/// 
/// Формулы:
/// - Скорость: v = V_dot × 4000 / (3600 × π × d_inner²)
/// - Число Рейнольдса: Re = 1000 × v × d_inner / ν
/// - Коэффициент трения: зависит от режима (Пуазейль или Колбрук-Уайт)
/// - Удельные потери: R = 10000 × (v² × ρ[г/см³] × λ) / (2 × d_inner) × 100
/// - Потери в трубе: Δp = R × L
/// - Потери в вентиле: Δp_Vent = (V_dot / 1000 / Kv)² × 100000 × ρ[г/см³]
/// 
/// Важно: Плотность ρ в формулах R и Δp_Vent должна быть в г/см³!
/// GlycolProperties.Density хранит плотность в кг/м³, требуется конвертация.
/// </remarks>
```

---

## 4. Тест-кейсы

### 4.1. Тест: Корректные удельные потери

**Входные данные**:
- velocity = 0.59 м/с
- density = 1053 кг/м³ (= 1.053 г/см³)
- frictionFactor = 0.042
- innerDiameter = 13 мм

**Ожидаемый результат**: 592 Па/м (±1)

**Формула**:
```
R = 10000 × (0.59² × 1.053 × 0.042) / (2 × 13) × 100
R = 10000 × (0.3481 × 1.053 × 0.042) / 26 × 100
R = 10000 × 0.01539 / 26 × 100
R = 10000 × 0.000592 × 100
R = 592 Па/м
```

### 4.2. Тест: Проверка отсутствия ошибки ×1000

**Входные данные**: те же

**Проверка**: Результат НЕ должен быть 592000 Па/м (ошибка ×1000)

---

## 5. Критерии приёмки

- [ ] Удельные потери рассчитываются с плотностью в **г/см³**
- [ ] Результат соответствует формуле: `R = 10000 × (v² × ρ[г/см³] × λ) / (2 × d_inner) × 100`
- [ ] При v = 0.59 м/с, ρ = 1053 кг/м³, λ = 0.042, d = 13 мм
  - Результат: **592 Па/м** (не 592000 Па/м)
- [ ] XML-комментарий обновлён с указанием единиц измерения
- [ ] Unit-тесты проходят

---

## 6. Ссылки

- **ТЗ**: `Work/Hydraulics/technical_specification_units.md` (раздел 2.3, 4.2)
- **Формулы**: `docs/Formulas_Snegotayanie.md` (раздел 11.8)
- **Код**: `src/Services/Hydraulics/CircuitsCalculator.cs` (строки 200-203)

---

*Дата создания: 2026-03-20*