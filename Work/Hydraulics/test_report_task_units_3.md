# Отчёт о тестировании задачи: Исправить ошибку #3 — Потери в вентиле

## Метаданные

| Параметр | Значение |
|----------|----------|
| **ID задачи** | task_units_3 |
| **Дата тестирования** | 2026-03-20 |
| **Статус** | ✅ Успешно |

---

## 1. Краткое описание

Исправлена ошибка в расчёте потерь давления в вентиле — плотность теплоносителя теперь конвертируется из кг/м³ в г/см³ перед использованием в формулах.

---

## 2. Изменённые файлы

### 2.1. `src/Services/Hydraulics/CircuitsCalculator.cs`

**Изменения:**

1. **Строка 182**: Конвертация плотности при присвоении в `CircuitTemperatureResult.Density`
   ```csharp
   Density = glycolProps.Density / 1000.0,  // Конвертация: кг/м³ → г/см³
   ```

2. **Строка 205**: Объявление переменной `density_g_cm3`
   ```csharp
   double density_g_cm3 = glycolProps.Density / 1000.0;
   ```

3. **Строки 206-207**: Использование `density_g_cm3` в расчёте удельных потерь
   ```csharp
   double pressureLossPerMeter = 10000 * Math.Pow(velocity, 2) * density_g_cm3 * frictionFactor
       / (2 * innerDiameter) * 100;
   ```

4. **Строка 218**: Использование `density_g_cm3` в расчёте потерь в вентиле
   ```csharp
   result.ValveLoss = Math.Pow(circuit.FlowRate / 1000.0 / kv, 2) * 100000 * density_g_cm3;
   ```

---

## 3. Результаты тестирования

### 3.1. Новые тесты

| Тест | Описание | Результат |
|------|----------|-----------|
| `CalculateAtTemperature_ConvertsDensityFromKgPerM3ToGPerCm3` | Проверка конвертации плотности из кг/м³ в г/см³ | ✅ PASSED |
| `CalculateAtTemperature_PressureLossPerMeter_UsesDensityInGramsPerCm3` | Проверка расчёта удельных потерь с плотностью в г/см³ | ✅ PASSED |
| `CalculateAtTemperature_ValveLoss_UsesDensityInGramsPerCm3` | Проверка расчёта потерь в вентиле с плотностью в г/см³ | ✅ PASSED |

### 3.2. Регрессионные тесты

| Категория | Количество | Пройдено | Провалено |
|-----------|------------|----------|-----------|
| CalculateCircuitPower | 6 | 6 | 0 |
| CalculateFlowRate | 5 | 5 | 0 |
| CalculateAtTemperature | 8 | 8 | 0 |
| CalculateAllCircuits | 6 | 6 | 0 |
| CalculateBalancing | 4 | 4 | 0 |
| CalculateCollectorSummary | 5 | 5 | 0 |
| **Всего** | **34** | **34** | **0** |

---

## 4. Проверка критериев приёмки

### 4.1. ✅ Потери в вентиле вычисляются в Па

**Тест**: `CalculateAtTemperature_ValveLoss_UsesDensityInGramsPerCm3`

**Входные данные:**
- V_dot = 280 л/ч
- ρ = 1053 кг/м³ (= 1.053 г/см³)
- Kv = 1.2 м³/ч

**Ожидаемый результат**: 5729 Па (±100 Па)

**Фактический результат**: ✅ 5729 Па (в пределах допуска)

**Формула:**
```
Δp_Vent = (280 / 1000 / 1.2)² × 100000 × 1.053
Δp_Vent = (0.233)² × 100000 × 1.053
Δp_Vent = 0.0544 × 100000 × 1.053
Δp_Vent = 5729 Па
```

### 4.2. ✅ Результат НЕ в 1000 раз больше

**Проверка**: Результат < 10000 Па

**Фактический результат**: ✅ 5729 Па < 10000 Па

**Примечание**: Если бы плотность не конвертировалась, результат был бы ≈ 5728272 Па (в 1000 раз больше).

### 4.3. ✅ Существующий функционал не нарушен

**Тесты**: Все 34 теста `CircuitsCalculatorTests` прошли успешно.

---

## 5. Проверка формул

### 5.1. Удельные потери давления

**Формула**: `R = 10000 × (v² × ρ[г/см³] × λ) / (2 × d_inner) × 100`

**Тест**: `CalculateAtTemperature_PressureLossPerMeter_UsesDensityInGramsPerCm3`

**Входные данные:**
- v = 0.59 м/с
- ρ = 1053 кг/м³ (= 1.053 г/см³)
- λ = 0.042
- d = 13 мм

**Ожидаемый результат**: 592 Па/м (±50 Па/м)

**Фактический результат**: ✅ 592 Па/м (в пределах допуска)

### 5.2. Потери в вентиле

**Формула**: `Δp_Vent = (V_dot / 1000 / Kv)² × 100000 × ρ[г/см³]`

**Тест**: `CalculateAtTemperature_ValveLoss_UsesDensityInGramsPerCm3`

**Ожидаемый результат**: 5729 Па (±100 Па)

**Фактический результат**: ✅ 5729 Па (в пределах допуска)

---

## 6. Выводы

### 6.1. Исправления применены корректно

- ✅ Плотность конвертируется из кг/м³ в г/см³
- ✅ Переменная `density_g_cm3` используется для обоих расчётов (удельные потери и потери в вентиле)
- ✅ Результаты соответствуют формулам из документации

### 6.2. Тесты проходят успешно

- ✅ Все 34 теста `CircuitsCalculatorTests` прошли
- ✅ Новые тесты проверяют конвертацию плотности
- ✅ Регрессионные тесты не выявили проблем

### 6.3. Критерии приёмки выполнены

- ✅ Потери в вентиле вычисляются в Па
- ✅ При V_dot = 280 л/ч, Kv = 1.2, ρ = 1053 кг/м³ результат ≈ 5729 Па
- ✅ Существующий функционал не нарушен

---

## 7. Рекомендации

### 7.1. Документация

Рекомендуется обновить XML-комментарии в коде, чтобы явно указать единицы измерения:

```csharp
/// <remarks>
/// Важно: Плотность ρ в формулах R и Δp_Vent должна быть в г/см³!
/// GlycolProperties.Density хранит плотность в кг/м³, требуется конвертация.
/// 
/// Пример: 1053 кг/м³ = 1.053 г/см³
/// </remarks>
```

### 7.2. Модель CircuitTemperatureResult

Рекомендуется добавить комментарий к свойству `Density`:

```csharp
/// <summary>
/// Плотность теплоносителя, г/см³
/// </summary>
/// <remarks>
/// Внимание: GlycolProperties.Density хранит плотность в кг/м³.
/// При присвоении требуется конвертация: Density = glycolProps.Density / 1000.0
/// 
/// Пример: 1053 кг/м³ = 1.053 г/см³
/// </remarks>
public double Density { get; set; }
```

---

*Дата создания: 2026-03-20*
*Тестировщик: Автоматические тесты*