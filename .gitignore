# Temporary files
*.tmp
*.log
nul

# OpenCode internal; keep only project workflow commands
.opencode/*
!.opencode/commands/
.opencode/commands/*
!.opencode/commands/architecture-*.md

# IDE
.vs/
.vscode/
*.suo
*.user

# Build outputs
bin/
obj/
*.exe
*.dll
# Артефакты сборки — вне git (урок №18: publish/ протухает, сетап собирается
# напрямую ISCC из свежего publish; урок №19: бинарный шум publish/ не в коммит)
publish/
output/

# Cache
.ruff_cache/
docs/инструкция/tooling/node_modules/
tools/.icon-cache/

# Work files (original Excel/Word - not needed in repo)
Work/*.xls
Work/*.xlsx
Work/*.docx
Work/*.xlsm

# Trash
корзина/

# Agent orchestration artifacts (OpenCode / OMO)
.omo/

# Local agent/tooling scratch
.playwright-mcp/
.slim/
TestResults/
Target
console.log*

# Keep data files
!data/*.json
!data/*.yaml

# Вспомогательные рабочие материалы (брендбук, презентации, скриншоты, генераты) — вне git
docs/workspace/
