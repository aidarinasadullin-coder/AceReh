<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Единый DataGrid (Фаза 2 редизайна; переезд из Dictionary.xaml).
        План Ф2.2: hairline-сетка, строки 32–36 px (токен-выбор 34), заголовки
        без заливки (Caption/Gray.600 + нижняя hairline), виртуализация,
        FrozenColumnCount=1 (закреплённая «№» — к двухрежимной таблице Ф3).

        Канонические ключи в Ф2 ни одной вьюхой ещё не подключены (таблицы
        носят локальные стили; подключение — Ф3+): ключи переезжают как
        заготовка, «страницы не ломаются» выполняется тривиально (ревью Ф2).
        Мёртвые легаси-алиасы RehauDataGridHeaderStyle/RehauDataGridRowStyle
        удалены из Dictionary.xaml (0 использований).
    -->

    <!-- Заголовок: без заливки, Caption/Gray.600, только нижняя hairline.
         Определяется РАНЬШЕ DataGrid.Default (StaticResource без forward refs) -->
    <Style x:Key="DataGridColumnHeader.Default" TargetType="DataGridColumnHeader">
        <Setter Property="FontFamily" Value="{StaticResource FontFamily.Main}"/>
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Caption}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{DynamicResource Brand.Gray.600.Brush}"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brand.Gray.100.Brush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Строка: 34 px, hover — нейтральный, выделение — красный tint -->
    <Style x:Key="DataGridRow.Default" TargetType="DataGridRow">
        <Setter Property="FontFamily" Value="{StaticResource FontFamily.Main}"/>
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Body}"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="Foreground" Value="{DynamicResource Brand.Black.Brush}"/>
        <Setter Property="Height" Value="34"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource RehauBackgroundGrayBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource Brand.Red.Tint.Brush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Ячейка: прозрачная, без своей рамки (линии несёт сетка/заголовок) -->
    <Style x:Key="DataGridCell.Default" TargetType="DataGridCell">
        <Setter Property="FontFamily" Value="{StaticResource FontFamily.Main}"/>
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Body}"/>
        <Setter Property="Padding" Value="10,4"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource Brand.Black.Brush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Единый DataGrid: определяется ПОСЛЕ заголовка/строки/ячейки
         (StaticResource не поддерживает forward references) -->
    <Style x:Key="DataGrid.Default" TargetType="DataGrid">
        <Setter Property="FontFamily" Value="{StaticResource FontFamily.Main}"/>
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Body}"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brand.Gray.100.Brush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="RowBackground" Value="White"/>
        <Setter Property="AlternatingRowBackground" Value="White"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource Brand.Gray.100.Brush}"/>
        <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource DataGridColumnHeader.Default}"/>
        <Setter Property="RowStyle" Value="{StaticResource DataGridRow.Default}"/>
        <Setter Property="CellStyle" Value="{StaticResource DataGridCell.Default}"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <!-- Виртуализация + закреплённая первая колонка (№) -->
        <Setter Property="FrozenColumnCount" Value="1"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
        <Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel"/>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource Brand.Gray.300.Brush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
