<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Канонический ComboBox (Фаза 2 редизайна; переезд из Dictionary.xaml,
        ревью Ф2 — ADR-006). Эталон: tokens.css .f .in — высота 34 в ЯВНОМ
        ключе; implicit-стиль (без x:Key) задаёт только шаблон, кисти и
        ItemContainerStyle, никаких размеров (ADR-006, п.1).

        Не-editable шаблон (PART_ToggleButton/PART_Popup): IsEditable в
        приложении не используется.
    -->

    <!-- Стиль элементов списка: определяется РАНЬШЕ ComboBox.Default
         (StaticResource не поддерживает forward references) -->
    <Style x:Key="ComboBoxItem.Default" TargetType="ComboBoxItem">
        <Setter Property="FontFamily" Value="{StaticResource FontFamily.Main}"/>
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Body}"/>
        <Setter Property="Foreground" Value="{DynamicResource Brand.Black.Brush}"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="border"
                            Background="Transparent"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource RehauBackgroundGrayBrush}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource Brand.Red.Tint.Brush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ComboBox.Default" TargetType="ComboBox">
        <Setter Property="FontFamily" Value="{StaticResource FontFamily.Main}"/>
        <Setter Property="FontSize" Value="{StaticResource Font.Size.Body}"/>
        <Setter Property="Foreground" Value="{DynamicResource Brand.Black.Brush}"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brand.Gray.100.Brush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource ComboBoxItem.Default}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <!-- Внешняя hairline-рамка + внутреннее фокус-кольцо:
                             метрики не прыгают (ADR-006, п.4) -->
                        <Border x:Name="OuterBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource Radius.SM}"/>
                        <Border x:Name="FocusRing"
                                Margin="1"
                                BorderThickness="1"
                                BorderBrush="Transparent"
                                CornerRadius="{StaticResource Radius.SM}"
                                IsHitTestVisible="False"/>
                        <Grid Margin="{TemplateBinding Padding}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                            <Path x:Name="Chevron"
                                  Grid.Column="1"
                                  Data="M0,0 L5,5 L10,0"
                                  Stroke="{DynamicResource Brand.Gray.300.Brush}"
                                  StrokeThickness="1.5"
                                  Width="10" Height="5"
                                  Stretch="None"
                                  Margin="8,0,2,0"
                                  VerticalAlignment="Center"
                                  RenderTransformOrigin="0.5,0.5"/>
                        </Grid>
                        <!-- ToggleButton для управления раскрытием списка -->
                        <ToggleButton x:Name="PART_ToggleButton"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      Cursor="Hand"
                                      Focusable="False"
                                      ClickMode="Press">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="Transparent"/>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <Popup x:Name="PART_Popup"
                               AllowsTransparency="True"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}">
                            <Border Background="White"
                                    BorderBrush="{DynamicResource Brand.Gray.100.Brush}"
                                    BorderThickness="1"
                                    CornerRadius="{StaticResource Radius.SM}"
                                    Margin="0,4,0,0"
                                    MaxHeight="300">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="12" ShadowDepth="2" Direction="270"
                                                      Opacity="0.12" Color="Black"/>
                                </Border.Effect>
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{DynamicResource Brand.Gray.300.Brush}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="FocusRing" Property="BorderBrush" Value="{DynamicResource Brand.Red.Brush}"/>
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="Chevron" Property="RenderTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="180"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="OuterBorder" Property="Background" Value="{DynamicResource RehauBackgroundGrayBrush}"/>
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{DynamicResource Brand.Gray.100.Brush}"/>
                            <Setter TargetName="Chevron" Property="Stroke" Value="{DynamicResource Brand.Gray.100.Brush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- IMPLICIT: комбобоксы без стиля (в т.ч. ячейки DataGrid) получают бренд-вид;
         размеров не задаём — см. ADR-006 -->
    <Style TargetType="ComboBox" BasedOn="{StaticResource ComboBox.Default}"/>

    <!-- Явный: поле формы 34px (эталон .f .in) -->
    <Style x:Key="ComboBox.Field" TargetType="ComboBox" BasedOn="{StaticResource ComboBox.Default}">
        <Setter Property="Height" Value="34"/>
        <Setter Property="Padding" Value="10,0"/>
    </Style>

</ResourceDictionary>
