# Services Directory Documentation

## Overview

Этот каталог содержит сервисы приложения для расчёта систем снеготаяния.

## Structure

```
Services/
├── Climate/           # Сервисы климатических данных
│   ├── IClimateDataService.cs
│   └── ClimateDataService.cs
├── Construction/      # Сервисы конструкции
│   ├── IConstructionService.cs
│   ├── ConstructionService.cs
│   └── ConstructionValidator.cs
├── Thermal/           # Сервисы теплового расчёта
│   ├── IThermalCalculator.cs
│   └── ThermalCalculator.cs
└── Hydraulics/        # Сервисы гидравлического расчёта
    ├── IHydraulicCalculator.cs   # Интерфейс калькулятора гидравлики
    └── IGlycolDataService.cs     # Интерфейс сервиса данных гликоля
```

## Hydraulics Services

### IHydraulicCalculator

Интерфейс калькулятора гидравлического расчёта. Предоставляет методы для:

- `CalculateVelocity()` — расчёт скорости потока
- `CalculateReynoldsNumber()` — расчёт числа Рейнольдса
- `DetermineFlowRegime()` — определение режима течения
- `CalculateFrictionFactor()` — расчёт коэффициента трения λ
- `CalculatePressureLossPerMeter()` — расчёт удельных потерь давления
- `CalculateValvePressureLoss()` — расчёт потерь в вентиле
- `Calculate()` — полный гидравлический расчёт
- `CalculateBalancing()` — балансировка контуров

### IGlycolDataService

Интерфейс сервиса свойств гликолей. Предоставляет методы для:

- `GetDensity()` — получение плотности
- `GetSpecificHeat()` — получение удельной теплоёмкости
- `GetKinematicViscosity()` — получение кинематической вязкости
- `GetThermalConductivity()` — получение теплопроводности
- `GetProperties()` — получение всех свойств
- `IsTemperatureSupported()` — проверка температуры
- `IsConcentrationSupported()` — проверка концентрации
- `GetMinTemperature()` / `GetMaxTemperature()` — диапазон температур
- `GetMinConcentration()` / `GetMaxConcentration()` — диапазон концентраций

## Dependencies

- `SnowMeltingCalculator.Models.Hydraulics` — модели гидравлики
- `SnowMeltingCalculator.Models.Thermal` — модели теплового расчёта
- `SnowMeltingCalculator.Models.Construction` — модели конструкции

## Related Files

- `docs/Formulas_Snegotayanie.md` — формулы расчёта
- `data/glycol_data.json` — данные о свойствах гликолей
- `data/rehau_products.json` — данные о продукции РЕХАУ