# Behaviors

## Назначение
Папка содержит attached behaviors для WPF-контролов.

## Структура

### TextBoxBehavior.cs
Attached behavior для улучшения UX ввода чисел в TextBox.

**Функционал:**
1. **SelectAllOnFocus** - выделение всего текста при получении фокуса
2. **RestoreOnEscape** - восстановление исходного значения при нажатии Escape
3. **NormalizeDecimalSeparator** - замена точки и запятой на системный десятичный разделитель

**Использование в XAML:**
```xml
<TextBox behaviors:TextBoxBehavior.SelectAllOnFocus="True"
         behaviors:TextBoxBehavior.RestoreOnEscape="True"
         behaviors:TextBoxBehavior.NormalizeDecimalSeparator="True"/>
```

**Применено к полям:**
- ClimateView: AirTemperature, WindSpeed, Humidity, SnowfallIntensity

### DataGridBehavior.cs
Attached behavior для автоматического входа в режим редактирования при клике на ячейку DataGrid.

**Проблема:**
В стандартном WPF DataGrid для входа в режим редактирования требуется:
- Одинарный клик для выделения ячейки
- Ещё один клик для входа в редактирование

**Решение:**
Поведение `SingleClickEdit` позволяет войти в редактирование при первом же клике на ячейку.

**Использование в XAML:**
```xml
<DataGrid behaviors:DataGridBehavior.SingleClickEdit="True"
          ...>
```

**Применено к:**
- CircuitsView: DataGrid контуров

## Тесты
- `tests/SnowMeltingCalculator.Tests/Behaviors/TextBoxBehaviorTests.cs`
- `tests/SnowMeltingCalculator.Tests/Behaviors/DataGridBehaviorTests.cs`